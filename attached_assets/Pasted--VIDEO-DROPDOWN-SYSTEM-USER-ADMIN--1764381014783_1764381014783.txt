```
VIDEO DROPDOWN SYSTEM - USER + ADMIN

═══════════════════════════════════════════════════════════════════════════════
THE GOAL
═══════════════════════════════════════════════════════════════════════════════

When users or admins open the video library, they see dropdowns:

BY TECHNIQUE:
- Triangle Choke (47)
- Armbar (52)
- Rear Naked Choke (38)
- Knee Slice Pass (41)
- etc.

BY INSTRUCTOR:
- John Danaher (156)
- Roger Gracie (43)
- Marcelo Garcia (67)
- etc.

These numbers are a "wow" moment. They show library depth instantly.

═══════════════════════════════════════════════════════════════════════════════
REQUIREMENTS
═══════════════════════════════════════════════════════════════════════════════

1. ALL 744 VIDEOS MUST APPEAR IN COUNTS
   - Including all 304 position-agnostic videos
   - No video should be invisible or uncounted
   - Every video belongs to at least one technique category
   - Every video has an instructor

2. DROPDOWNS MUST BE DYNAMIC
   - When a new video is added → technique count updates automatically
   - When a new instructor is added → they appear in dropdown automatically
   - When a new technique type is tagged → it appears in dropdown automatically
   - NO manual updates required

3. APPLIES TO BOTH USER AND ADMIN
   - User-facing video library: same dynamic dropdowns
   - Admin dashboard: same dynamic dropdowns
   - Counts must match between both views

═══════════════════════════════════════════════════════════════════════════════
SHOW ME CURRENT STATE
═══════════════════════════════════════════════════════════════════════════════

1. USER DROPDOWN - List all technique categories with counts
2. USER DROPDOWN - List all instructors with counts
3. ADMIN DROPDOWN - List all technique categories with counts
4. ADMIN DROPDOWN - List all instructors with counts
5. TOTAL COUNT CHECK - Do all counts add up to 744?

═══════════════════════════════════════════════════════════════════════════════
FIX IF NEEDED
═══════════════════════════════════════════════════════════════════════════════

If any videos are missing from dropdowns:
1. Identify which videos are not appearing
2. Fix the query/logic so ALL videos are included
3. Ensure position-agnostic videos appear under their technique name

If dropdowns are static (hardcoded):
1. Convert to dynamic queries
2. Pull directly from database
3. Auto-update when videos are added/edited

═══════════════════════════════════════════════════════════════════════════════
VERIFICATION TEST
═══════════════════════════════════════════════════════════════════════════════

After fixes, run this test:

1. Add a test video with:
   - Technique: "Buggy Choke"
   - Instructor: "Craig Jones"
   
2. Refresh user dropdown - does "Buggy Choke" appear with count?
3. Refresh admin dropdown - does "Craig Jones" count increase?
4. Delete test video
5. Confirm counts return to original

If this works automatically, the system is correct.

═══════════════════════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════════════════════

1. Current dropdown output (user + admin) with all counts
2. Total video count verification (should equal 744)
3. List any missing videos or gaps
4. Confirmation that dropdowns are DYNAMIC (auto-update)
5. Screenshot or code showing the fix if changes were needed
```