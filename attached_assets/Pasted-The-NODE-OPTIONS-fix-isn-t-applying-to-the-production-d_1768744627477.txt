The NODE_OPTIONS fix isn’t applying to the **production deployment** - only to the dev environment. The heap total is still 116.3 MB instead of 7GB.

**Send this to Replit Agent:**

```
TWO CRITICAL ISSUES THAT MUST BE FIXED NOW:

═══════════════════════════════════════════════════════════════════════════════
ISSUE 1: MEMORY - NODE_OPTIONS NOT APPLIED TO PRODUCTION
═══════════════════════════════════════════════════════════════════════════════

I'm still getting memory alerts at 88.7% with Heap Total of only 116.3 MB.

We set NODE_OPTIONS="--max-old-space-size=7168" but it's NOT working in production.

The DEPLOYMENT environment variables are different from the dev environment.

FIX THIS:
1. Go to Deployments → Settings → Environment Variables
2. Add NODE_OPTIONS="--max-old-space-size=7168" to the DEPLOYMENT secrets (not just dev secrets)
3. Redeploy/Republish the production deployment
4. Verify the heap limit is 7GB in production by checking the startup logs

The dev environment showed 7GB but production is still at 116MB. This means the env var isn't in the deployment.

═══════════════════════════════════════════════════════════════════════════════
ISSUE 2: ADMIN DASHBOARD STUCK ON "LOADING METRICS..."
═══════════════════════════════════════════════════════════════════════════════

The admin dashboard has been broken for hours. It shows "Loading metrics..." and then goes blank.

Videos and Command Center work. Only the main dashboard is broken.

DO NOT tell me it's fixed. Actually fix it:

1. Test the /api/admin/metrics endpoint directly - what does it return?
2. Check for JavaScript errors in the browser console
3. Check server logs for errors when dashboard loads
4. Show me the ACTUAL error message
5. Fix the root cause

═══════════════════════════════════════════════════════════════════════════════
DO BOTH OF THESE NOW
═══════════════════════════════════════════════════════════════════════════════

1. Add NODE_OPTIONS to DEPLOYMENT environment variables and republish
2. Fix the admin dashboard and verify it actually loads

Do not mark this complete until both are actually working.
```