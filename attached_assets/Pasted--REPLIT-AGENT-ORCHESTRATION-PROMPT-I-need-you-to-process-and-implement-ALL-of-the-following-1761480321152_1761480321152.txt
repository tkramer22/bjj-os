# 🎯 REPLIT AGENT ORCHESTRATION PROMPT

```
I need you to process and implement ALL of the following system prompts simultaneously without stopping to ask for clarification unless absolutely critical. These prompts define Professor OS's complete personality and capabilities:

1. INSTRUCTOR KNOWLEDGE BASE (Complete 50+ instructor system)
2. DIAGNOSTIC INTELLIGENCE & ERROR CORRECTION
3. PROGRESSIVE SKILL DEVELOPMENT
4. COMPETITION MODE
5. GENERAL CONVERSATION MODE
6. [Any other prompts you uploaded]

═══════════════════════════════════════════════════════════════
EXECUTION INSTRUCTIONS FOR REPLIT AGENT
═══════════════════════════════════════════════════════════════

**YOUR TASK:**
Integrate ALL provided system prompts into the Claude API integration for Professor OS. These prompts work together as a complete system - do not treat them as separate options.

**CRITICAL REQUIREMENTS:**

1. **DO NOT ASK FOR CLARIFICATION** unless you encounter:
   - Conflicting technical requirements that cannot be resolved
   - Missing critical environment variables or API keys
   - Actual code errors that block implementation
   - Security concerns that require user decision

2. **AUTONOMOUS EXECUTION:**
   - Read all provided prompts
   - Understand how they work together
   - Implement them as a cohesive system
   - Make reasonable technical decisions
   - Use best practices for prompt engineering

3. **IMPLEMENTATION APPROACH:**
   - Combine all prompts into the system message for Claude API
   - Preserve the complete structure and content of each prompt
   - Ensure proper formatting (maintain all the ═ separators and structure)
   - Test that the combined prompt doesn't exceed token limits
   - If token limits are an issue, use prompt compression techniques

4. **WHERE TO IMPLEMENT:**
   Location: `/server/coaching.js` (or wherever Claude API calls are made)
   
   The system prompt should be sent in the Claude API call like this:
   
   ```javascript
   const response = await anthropic.messages.create({
     model: 'claude-sonnet-4-5-20250929',
     max_tokens: 4096,
     system: COMPLETE_PROFESSOR_OS_SYSTEM_PROMPT, // <-- All prompts combined here
     messages: [
       {
         role: 'user',
         content: userMessage
       }
     ]
   });
```

1. **HOW TO COMBINE PROMPTS:**
   
   Create a single system prompt that includes all sections:
   
   ```javascript
   const PROFESSOR_OS_SYSTEM_PROMPT = `
   You are Professor OS, the world's most intelligent BJJ coaching system.
   
   ${INSTRUCTOR_KNOWLEDGE_BASE}
   
   ${DIAGNOSTIC_INTELLIGENCE_FRAMEWORK}
   
   ${PROGRESSIVE_SKILL_DEVELOPMENT}
   
   ${COMPETITION_MODE}
   
   ${GENERAL_CONVERSATION_MODE}
   
   ${POST_SESSION_COACHING}
   
   ${PRE_TRAINING_FOCUS}
   
   ${TECHNIQUE_QUERY_MODE}
   
   [All other prompt sections]
   
   ═══════════════════════════════════════════════════════════════
   FINAL CRITICAL INSTRUCTION
   ═══════════════════════════════════════════════════════════════
   
   Apply ALL of the above frameworks simultaneously. Choose the appropriate 
   mode based on user input, but always maintain instructor knowledge, 
   diagnostic intelligence, and coaching excellence across all interactions.
   
   Excellence is your pillar.
   `;
   ```
1. **WHAT YOU SHOULD DO AUTOMATICALLY:**

- Find the correct file where Claude API is called
- Create or update the system prompt variable
- Combine all provided prompts into one cohesive system message
- Ensure proper string escaping and formatting
- Test that it works with a sample API call
- Verify token count is within limits (if over ~100K tokens, compress)

1. **ONLY STOP AND ASK IF:**

- Claude API key is missing and you can’t find it in environment variables
- The combined prompt exceeds API limits and compression isn’t sufficient
- There’s a critical technical conflict in the implementation
- You discover the file structure is completely different than expected

1. **TESTING AFTER IMPLEMENTATION:**
   After implementing, test with these sample queries to ensure all modes work:

- “I just finished training” (should trigger post-session mode)
- “I’m competing in 3 weeks” (should trigger competition mode)
- “How do I escape side control?” (should trigger technique query with instructor citations)
- “I’m feeling discouraged” (should trigger general conversation with empathy)

═══════════════════════════════════════════════════════════════
DELIVERABLE
═══════════════════════════════════════════════════════════════

When complete, provide:

1. ✅ Confirmation that all prompts are integrated
1. 📁 File path where implementation was done
1. 🧪 Test results showing it works
1. ⚠️ Any warnings or notes about token usage
1. 📊 Estimated token count of combined system prompt

DO NOT provide the full code unless I specifically ask. Just confirm completion and provide summary.

═══════════════════════════════════════════════════════════════
BEGIN IMPLEMENTATION NOW
═══════════════════════════════════════════════════════════════

Process all provided system prompts and integrate them into Professor OS’s Claude API calls immediately. Only interrupt if you hit a genuine blocker that requires my input.

```
---

## 📋 COPY THIS - SINGLE MESSAGE VERSION
```

Integrate ALL provided system prompts into Professor OS’s Claude API integration immediately. Only stop to ask questions if you encounter actual blockers (missing API keys, conflicting technical requirements, file structure completely different than expected). Otherwise, execute autonomously:

TASK: Combine all prompts into a single system message for Claude API calls in /server/coaching.js (or equivalent file)

REQUIREMENTS:

- Read all provided prompts
- Combine into cohesive system prompt
- Implement in Claude API call
- Test with sample queries
- Only ask if genuine blocker

DELIVERABLE:
✅ Confirmation of integration
📁 File path
🧪 Test results
⚠️ Token count/warnings

BEGIN NOW.

```
---

**This tells Replit Agent to:**
1. ✅ Process all prompts at once
2. ✅ Stop asking permission
3. ✅ Only interrupt for genuine blockers
4. ✅ Implement autonomously
5. ✅ Test the implementation
6. ✅ Report back when done

**Copy either version (detailed or short) and send to Replit Agent!**
```