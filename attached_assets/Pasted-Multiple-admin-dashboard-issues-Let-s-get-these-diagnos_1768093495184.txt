Multiple admin dashboard issues. Let’s get these diagnosed and fixed. Send this to Replit:

```
ADMIN DASHBOARD - MULTIPLE ISSUES TO FIX

Several admin panel features are not working correctly. Need to diagnose and fix each one.

═══════════════════════════════════════════════════════════════════════════════
ISSUE 1: CURATION SHOWS "OFFLINE" WHEN IT'S NOT
═══════════════════════════════════════════════════════════════════════════════

- Dashboard shows "Curation Offline" even after turning auto curation ON
- "Curation efficiency today" shows nothing was curated (but videos WERE curated manually)
- Command center shows "nothing was discovered" which is incorrect

DIAGNOSE:
1. What triggers the "online/offline" status display?
2. Is it checking the wrong flag or table?
3. Why aren't manually curated videos counting in the stats?

FIX: The dashboard should accurately reflect:
- Whether auto curation is enabled (on/off toggle status)
- Total videos curated today (manual + auto)
- Videos discovered vs added

═══════════════════════════════════════════════════════════════════════════════
ISSUE 2: USER ACTIVITY TAB NOT WORKING
═══════════════════════════════════════════════════════════════════════════════

The User Activity tab in admin dashboard does not load or display data.

DIAGNOSE:
1. Is the API endpoint for user activity returning data?
2. Is there a frontend error preventing render?
3. Check console logs for errors on that tab

FIX: User Activity should show:
- Active users
- Session counts
- Recent activity

═══════════════════════════════════════════════════════════════════════════════
ISSUE 3: USER REQUESTS NOT SHOWING (META SECTION)
═══════════════════════════════════════════════════════════════════════════════

A new user was created but no user requests are showing in the meta/analytics section.

DIAGNOSE:
1. Where are user requests logged?
2. Is the new user's activity being tracked?
3. What should appear in the "user requests" section?

FIX: Should display:
- New user signups
- User requests/queries to Professor OS
- Any user feedback or interactions

═══════════════════════════════════════════════════════════════════════════════
PRIORITY ORDER
═══════════════════════════════════════════════════════════════════════════════

1. Fix curation status display (most visible issue)
2. Fix user activity tab
3. Fix user requests tracking

Show me what you find for each issue before making changes.
```

-----

This covers all three issues you mentioned. Let me know what Replit finds! 🤙​​​​​​​​​​​​​​​​