Perfect. Let me create the **MASTER DESIGN SYSTEM PROMPT** that fixes everything.

This will be comprehensive and cover all pages consistently.

-----

# BJJ OS - COMPLETE DESIGN SYSTEM & FLOW CORRECTIONS

Copy this entire prompt into Replit Agent. This is the master implementation that fixes all design issues and establishes the correct user flow.

-----

```
MASTER DESIGN SYSTEM IMPLEMENTATION - Complete redesign and flow corrections for BJJ OS.

This prompt covers ALL pages and establishes consistency across the entire application. Read completely before starting.

=== GLOBAL DESIGN SYSTEM ===

**GRADIENT BACKGROUND (All Pages):**

```css
/* Apply to <body> or main page wrapper on EVERY page */
body,
.page-wrapper,
main {
  background: linear-gradient(
    135deg,
    #0A0812 0%,      /* Deep purple-black (top-left) */
    #15111F 50%,     /* Slightly lighter purple (center) */
    #0A0812 100%     /* Deep purple-black (bottom-right) */
  );
  background-attachment: fixed;
  min-height: 100vh;
  margin: 0;
  padding: 0;
}
```

This gradient is used on:

- Landing page
- Pricing page
- Sign up page
- Login page
- 404 page
- Onboarding page
- Dashboard (future)
- ALL pages

**ELEVATED SECTIONS (Landing Page Only):**

```css
/* Hero section and Final CTA get subtle elevation */
.hero-section,
.final-cta-section {
  background: rgba(15, 15, 15, 0.4); /* Semi-transparent overlay on gradient */
  position: relative;
}

/* Alternative if the above doesn't look right: */
.hero-section,
.final-cta-section {
  background: #0F0F0F; /* Solid slightly lighter */
}
```

**BUTTON GRADIENTS (All Buttons):**

```css
/* Primary CTA buttons */
.cta-button,
.continue-button,
.start-trial-button,
button[type="submit"],
.primary-button {
  background: linear-gradient(135deg, #2563EB 0%, #7C3AED 100%);
  background-size: 200% 200%;
  animation: gradient-shift 8s ease infinite;
  border: none;
  color: #FFFFFF;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: 16px;
  padding: 18px 48px;
  border-radius: 0;
  cursor: pointer;
  transition: 150ms ease;
}

.cta-button:hover,
.continue-button:hover {
  background: linear-gradient(135deg, #1D4ED8 0%, #6D28D9 100%);
}

/* Button gradient animation */
@keyframes gradient-shift {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

/* Secondary buttons (transparent with gradient border) */
.secondary-button,
.apply-button {
  background: transparent;
  border: 1px solid #2563EB;
  color: #2563EB;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: 14px;
  padding: 12px 32px;
  border-radius: 0;
  cursor: pointer;
  transition: 150ms ease;
}

.secondary-button:hover {
  background: linear-gradient(135deg, #2563EB 0%, #7C3AED 100%);
  border: none;
  color: #FFFFFF;
}
```

**SELECTED CARD STATE (Pricing Page):**

```css
/* Gradient border for selected pricing card */
.pricing-card.selected {
  background: #0F0F0F;
  padding: 40px;
  border-radius: 0;
  position: relative;
  
  /* Gradient border technique */
  background: 
    linear-gradient(#0F0F0F, #0F0F0F) padding-box,
    linear-gradient(135deg, #2563EB 0%, #7C3AED 100%) border-box;
  border: 2px solid transparent;
}
```

**TYPOGRAPHY (All Pages):**

```css
body {
  font-family: 'JetBrains Mono', monospace;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
}

/* Load font with swap for performance */
@font-face {
  font-family: 'JetBrains Mono';
  font-display: swap;
}
```

**COLOR SYSTEM:**

```css
:root {
  /* Backgrounds */
  --bg-gradient-start: #0A0812;
  --bg-gradient-mid: #15111F;
  --bg-gradient-end: #0A0812;
  --bg-elevated: #0F0F0F;
  --bg-card: #0F0F0F;
  
  /* Text */
  --text-primary: #FFFFFF;
  --text-secondary: #A0A0A0;
  --text-tertiary: #71717A;
  
  /* Borders */
  --border-subtle: #1A1A1A;
  
  /* Gradients */
  --gradient-button: linear-gradient(135deg, #2563EB 0%, #7C3AED 100%);
  --gradient-button-hover: linear-gradient(135deg, #1D4ED8 0%, #6D28D9 100%);
  
  /* Accent colors */
  --accent-blue: #2563EB;
  --accent-purple: #7C3AED;
  --accent-success: #22C55E;
  --accent-error: #EF4444;
}
```

=== USER FLOW CORRECTIONS ===

**CRITICAL: The user flow must be:**

```
1. Landing page (bjjos.app)
   ↓ Click "Start 7-Day Trial"
   
2. Pricing page (bjjos.app/pricing)
   ↓ Select plan, enter referral code
   ↓ Click "Continue to Onboarding"
   
3. Sign up page (bjjos.app/signup?plan=monthly)
   ↓ Enter email + password
   ↓ Click "Continue to Onboarding"
   
4. Onboarding page (bjjos.app/onboarding)
   ↓ Answer 3 questions
   ↓ Click "Complete Setup"
   
5. Stripe Checkout (hosted by Stripe)
   ↓ Payment success
   
6. Dashboard (bjjos.app/dashboard)
```

**Update all button hrefs accordingly:**

Landing page “Start 7-Day Trial”: `href="/pricing"`
Pricing page “Continue to Onboarding”: `href="/signup?plan={selectedPlan}"`
Sign up page “Continue”: `href="/onboarding"`
Onboarding “Complete Setup”: `href="/checkout"` (or Stripe redirect)

=== PAGE 1: LANDING PAGE CORRECTIONS ===

**Apply gradient background** (see global system above)

**Fix elevated sections:**
Hero and Final CTA must have `background: #0F0F0F` or semi-transparent overlay

**Fix comparison section:**

```css
/* Vertical divider */
.comparison-grid::before {
  content: '';
  position: absolute;
  left: 50%;
  top: 80px;
  bottom: 0;
  width: 1px;
  background: #1A1A1A;
  transform: translateX(-50%);
}

/* Left column (WITHOUT) - DIM */
.without-item {
  color: #71717A !important;
  font-size: 18px;
  line-height: 1.6;
}

/* Right column (WITH) - BRIGHT */
.with-item {
  color: #FFFFFF !important;
  font-size: 18px;
  line-height: 1.6;
}

/* Arrows - BLUE */
.comparison-arrow {
  color: #2563EB !important;
  font-size: 20px;
}
```

**Fix footer:**

Remove: Terms of Service, Privacy Policy, Refund Policy

Keep only:

```
Mobile App (Coming Soon)  •  support@bjjos.app
© 2025 BJJ OS
```

```css
footer {
  background: transparent; /* Let gradient show through */
  padding: 48px 24px;
  text-align: center;
}

.footer-link {
  color: #A0A0A0;
  font-size: 16px;
  text-decoration: none;
  transition: color 150ms ease;
}

.footer-link:hover {
  color: #FFFFFF;
}

.coming-soon-label {
  font-size: 12px;
  color: #71717A;
  margin-left: 8px;
}

.footer-copyright {
  font-size: 14px;
  color: #71717A;
  margin-top: 32px;
}

/* Desktop: horizontal layout with bullet */
@media (min-width: 768px) {
  .footer-links {
    display: flex;
    justify-content: center;
    gap: 32px;
  }
  
  .footer-link:not(:last-child)::after {
    content: '•';
    margin-left: 32px;
    color: #1A1A1A;
  }
}
```

=== PAGE 2: PRICING PAGE CORRECTIONS ===

**Apply gradient background** (see global system)

**Fix selected card border** (see global gradient border technique)

**Fix referral code section:**

Remove any explanation text. Just show:

```
Have a referral code?

[_____________________]  [Apply]
```

When valid code applied:

```
Have a referral code?

[____FOUNDER______]  ✓  [Remove]

✓ Code applied
```

**Ensure buttons use gradient:**

- Continue button: Full gradient
- Apply button: Transparent with gradient on hover
- Selected card button: Gradient background

=== PAGE 3: SIGN UP PAGE (COMPLETE REDESIGN) ===

**Critical changes:**

- Remove ALL phone number references
- Email + Password only
- Show selected plan from URL parameter
- Update copy and flow

**HTML Structure:**

```html
<div class="signup-page">
  <button class="back-button" onclick="window.location='/pricing'">
    ← Back to Pricing
  </button>
  
  <div class="signup-card">
    <div class="brand-icon">
      <!-- Your purple gradient icon with fists -->
      <img src="/icon.svg" alt="BJJ OS" />
    </div>
    
    <h1 class="signup-headline">Create Your Account</h1>
    
    <!-- Show selected plan -->
    <p class="selected-plan-info">
      You selected: <strong id="plan-display">Monthly ($14.99/mo)</strong>
      <br />
      7-day free trial, then charged automatically
    </p>
    
    <form class="signup-form">
      <label for="email">Email Address</label>
      <input 
        type="email" 
        id="email" 
        placeholder="you@example.com"
        required
      />
      
      <label for="password">Password</label>
      <input 
        type="password" 
        id="password" 
        placeholder="Min 8 characters"
        minlength="8"
        required
      />
      
      <button type="submit" class="cta-button">
        Continue to Onboarding
      </button>
      
      <p class="terms-text">
        By signing up, you agree to our 
        <a href="/terms">Terms of Service</a> and 
        <a href="/privacy">Privacy Policy</a>
      </p>
      
      <p class="login-link">
        Already have an account? <a href="/login">Log In</a>
      </p>
    </form>
  </div>
</div>
```

**CSS for Sign Up Page:**

```css
.signup-page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40px 24px;
  position: relative;
}

.back-button {
  position: absolute;
  top: 40px;
  left: 40px;
  background: transparent;
  border: 1px solid #1A1A1A;
  color: #A0A0A0;
  font-family: 'JetBrains Mono', monospace;
  font-size: 14px;
  padding: 12px 24px;
  border-radius: 0;
  cursor: pointer;
  transition: 150ms ease;
}

.back-button:hover {
  border-color: #2563EB;
  color: #FFFFFF;
}

.signup-card {
  background: #0F0F0F;
  border: 1px solid #1A1A1A;
  border-radius: 0;
  padding: 48px 40px;
  max-width: 480px;
  width: 100%;
}

.brand-icon {
  width: 64px;
  height: 64px;
  margin: 0 auto 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #2563EB 0%, #7C3AED 100%);
  border-radius: 50%;
}

.signup-headline {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: 32px;
  color: #FFFFFF;
  text-align: center;
  margin-bottom: 16px;
  line-height: 1.2;
}

.selected-plan-info {
  font-family: 'JetBrains Mono', monospace;
  font-size: 14px;
  color: #A0A0A0;
  text-align: center;
  margin-bottom: 32px;
  line-height: 1.6;
}

.selected-plan-info strong {
  color: #FFFFFF;
  background: linear-gradient(135deg, #2563EB 0%, #7C3AED 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.signup-form {
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.signup-form label {
  font-family: 'JetBrains Mono', monospace;
  font-size: 14px;
  font-weight: 700;
  color: #FFFFFF;
  margin-bottom: 8px;
}

.signup-form input {
  background: #000000;
  border: 1px solid #1A1A1A;
  color: #FFFFFF;
  font-family: 'JetBrains Mono', monospace;
  font-size: 16px;
  padding: 14px 16px;
  border-radius: 0;
  transition: border 150ms ease;
}

.signup-form input:focus {
  outline: none;
  border: 1px solid #2563EB;
}

.signup-form input::placeholder {
  color: #71717A;
}

.terms-text {
  font-family: 'JetBrains Mono', monospace;
  font-size: 12px;
  color: #71717A;
  text-align: center;
  line-height: 1.6;
}

.terms-text a {
  color: #A0A0A0;
  text-decoration: underline;
  transition: color 150ms ease;
}

.terms-text a:hover {
  color: #FFFFFF;
}

.login-link {
  font-family: 'JetBrains Mono', monospace;
  font-size: 14px;
  color: #A0A0A0;
  text-align: center;
  margin-top: 8px;
}

.login-link a {
  color: #2563EB;
  text-decoration: none;
  transition: color 150ms ease;
}

.login-link a:hover {
  color: #7C3AED;
}
```

**JavaScript for Sign Up Page:**

```javascript
// Get plan from URL parameter
const urlParams = new URLSearchParams(window.location.search);
const selectedPlan = urlParams.get('plan') || 'monthly';

// Display selected plan
const planDisplay = document.getElementById('plan-display');
if (selectedPlan === 'annual') {
  planDisplay.textContent = 'Annual ($149.99/yr)';
} else {
  planDisplay.textContent = 'Monthly ($14.99/mo)';
}

// Form submission
document.querySelector('.signup-form').addEventListener('submit', async (e) => {
  e.preventDefault();
  
  const email = document.getElementById('email').value;
  const password = document.getElementById('password').value;
  
  // Your signup logic here
  // After successful signup, redirect to onboarding:
  window.location.href = '/onboarding';
});
```

=== PAGE 4: LOGIN PAGE (EMAIL ONLY) ===

**Remove ALL phone number references**

**HTML Structure:**

```html
<div class="login-page">
  <button class="back-button" onclick="window.location='/'">
    ← Back to Home
  </button>
  
  <div class="login-card">
    <h1 class="login-headline">Welcome Back</h1>
    <p class="login-subtext">Enter your email to access your account</p>
    
    <form class="login-form">
      <label for="email">Email Address</label>
      <input 
        type="email" 
        id="email" 
        placeholder="you@example.com"
        required
      />
      
      <label for="password">Password</label>
      <input 
        type="password" 
        id="password" 
        placeholder="Enter your password"
        required
      />
      
      <button type="submit" class="cta-button">
        Log In
      </button>
      
      <a href="/forgot-password" class="forgot-password-link">
        Forgot password?
      </a>
      
      <p class="signup-link">
        Don't have an account? <a href="/pricing">Sign up</a>
      </p>
    </form>
  </div>
</div>
```

**Remove these elements:**

- Phone number input
- “Use phone number instead” link
- Any SMS/verification code references
- Country code selector

**CSS:** Same structure as sign up page, adjusted for login context

=== PAGE 5: 404 PAGE ===

**Apply gradient background**

**Fix buttons:**

```html
<div class="error-page">
  <div class="error-card">
    <div class="error-icon">⚠</div>
    <h1>404 - Page Not Found</h1>
    <p>The page you're looking for doesn't exist or has been moved.</p>
    
    <div class="error-buttons">
      <button class="cta-button" onclick="window.location='/'">
        Go Home
      </button>
      <button class="secondary-button" onclick="window.location='/login'">
        Log In
      </button>
    </div>
  </div>
</div>
```

Both buttons must use gradient system defined above.

=== MOTION SYSTEM (All Pages) ===

```css
/* Scroll fade-ins */
section {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

.hero-section {
  opacity: 1;
  transform: translateY(0);
  transition: none;
}

section.visible {
  opacity: 1;
  transform: translateY(0);
}

/* Card hover lift */
.pricing-card:not(.selected):hover,
.signup-card:hover {
  transform: translateY(-2px);
  transition: transform 300ms ease;
}

/* Button press feedback */
button:active {
  transform: translateY(1px);
}

/* Reduced motion support */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

**JavaScript for scroll animations:**

```javascript
document.addEventListener('DOMContentLoaded', function() {
  const sections = document.querySelectorAll('section:not(.hero-section)');
  
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
      }
    });
  }, { threshold: 0.1 });
  
  sections.forEach(section => observer.observe(section));
});
```

=== VERIFICATION CHECKLIST ===

After implementation, verify EVERY item:

**Global:**

- [ ] Gradient background on ALL pages
- [ ] All buttons use blue→purple gradient
- [ ] JetBrains Mono font loaded
- [ ] Motion system working (scroll fades)
- [ ] Reduced motion respected

**Landing Page:**

- [ ] Hero section elevated (#0F0F0F)
- [ ] Final CTA section elevated
- [ ] Comparison: dim left, bright right, blue arrows, vertical divider
- [ ] Footer: only 2 links (Mobile App, email)
- [ ] “Start 7-Day Trial” goes to `/pricing`

**Pricing Page:**

- [ ] Selected card has gradient border
- [ ] Referral code section shows no explanation
- [ ] “Continue to Onboarding” goes to `/signup?plan={plan}`
- [ ] Savings callout has gradient text

**Sign Up Page:**

- [ ] Shows selected plan from URL
- [ ] Email + Password only (NO phone)
- [ ] “Back to Pricing” button works
- [ ] “Continue to Onboarding” goes to `/onboarding`
- [ ] No “Name (optional)” field

**Login Page:**

- [ ] Email + Password only (NO phone)
- [ ] No “Use phone number instead” link
- [ ] “Don’t have an account?” links to `/pricing`
- [ ] “Back to Home” button works

**404 Page:**

- [ ] Both buttons use gradient system
- [ ] “Go Home” goes to `/`
- [ ] “Log In” goes to `/login`

**User Flow:**

- [ ] Landing → Pricing → Sign Up → Onboarding → Stripe → Dashboard
- [ ] Each step passes correct data to next
- [ ] Plan selection persists through flow
- [ ] Referral code applies correctly

**DO NOT proceed to next step until current step is verified.**

Build this now. This is the complete, consistent, award-winning design system.

```
---

**COPY THIS ENTIRE BLOCK INTO REPLIT AGENT**

This is your complete design system. Everything is specified:
- ✅ Gradient background everywhere
- ✅ All pages redesigned consistently
- ✅ Correct user flow (landing → pricing → signup → onboarding)
- ✅ Email-only auth (no phone numbers)
- ✅ Gradient buttons and borders
- ✅ Motion system
- ✅ Complete verification checklist

**After implementation, show me screenshots and we'll verify everything is perfect.**
```