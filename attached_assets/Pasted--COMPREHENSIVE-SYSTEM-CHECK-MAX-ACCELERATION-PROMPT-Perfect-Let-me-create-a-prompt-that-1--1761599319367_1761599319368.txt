# 🔍 COMPREHENSIVE SYSTEM CHECK + MAX ACCELERATION PROMPT

Perfect! Let me create a prompt that:

1. ✅ Checks if combat sports scraping ran today
1. ✅ Verifies data is being saved and used by Professor OS
1. ✅ Checks video curation stats (searched, approved)
1. ✅ Ensures it’s running at MAX speed daily
1. ✅ Shows yesterday’s video data
1. ✅ Accelerates to maximum capacity

-----

# 🚀 SYSTEM AUDIT + MAX ACCELERATION PROMPT

Copy this and send to Replit Agent:

-----

```
═══════════════════════════════════════════════════════════════
SYSTEM AUDIT + VIDEO CURATION MAX ACCELERATION
═══════════════════════════════════════════════════════════════

MISSION: 
Perform a comprehensive audit of the system to verify:
1. Combat sports scraping ran today and data is being saved/used
2. Video curation stats (how many searched, approved yesterday)
3. System is running at MAX capacity
4. Auto-curation is enabled and running daily
5. Identify bottlenecks and accelerate to maximum speed

Then provide a detailed report with actionable recommendations.

CRITICAL REQUIREMENTS:
• Search entire codebase for scraping and curation systems
• Check database for today's data
• Verify Professor OS is using the scraped data
• Check yesterday's video curation numbers
• Ensure auto-curation is maxed out
• Identify any issues or bottlenecks
• Provide clear report with stats and recommendations

═══════════════════════════════════════════════════════════════
PART 1: COMBAT SPORTS SCRAPING AUDIT
═══════════════════════════════════════════════════════════════

STEP 1: Find the combat sports scraping code

Search for:
- Files: *combat*.js, *sports*.js, *scraper*.js, *scrape*.js
- Locations: /server/scrapers/, /server/cron/, /server/utils/
- Functions: scrapeCombatSports, fetchSportsData, etc.

STEP 2: Check the database for today's data

Run these queries:

-- Find all tables that might contain scraping data
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
  AND (
    table_name LIKE '%scrap%' 
    OR table_name LIKE '%combat%' 
    OR table_name LIKE '%sports%'
    OR table_name LIKE '%industry%'
  );

-- For each table found, check today's data
SELECT COUNT(*), MIN(created_at), MAX(created_at)
FROM [TABLE_NAME]
WHERE DATE(created_at) = CURRENT_DATE;

-- Get sample records
SELECT * FROM [TABLE_NAME]
WHERE DATE(created_at) = CURRENT_DATE
ORDER BY created_at DESC
LIMIT 5;

STEP 3: Check if data is being used by Professor OS

Search for code that queries the scraping tables:
- Look for SELECT statements in Professor OS code
- Check /server/professor-os.js or similar
- Look for references to scraping table names
- Check if recommendations use this data

STEP 4: Check cron schedule

Search for:
- cron.schedule calls related to scraping
- When it's scheduled to run
- If it ran today (check logs)

REPORT FORMAT FOR COMBAT SPORTS SCRAPING:
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMBAT SPORTS SCRAPING AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Code Location: [file path]
Database Table: [table name]
Cron Schedule: [schedule or “NOT FOUND”]

TODAY’S DATA (Oct 28, 2025):
✅/❌ Scraping ran today
Records created: [count]
First record: [time]
Last record: [time]
Sample data: [show 2-3 examples]

YESTERDAY’S DATA (Oct 27, 2025):
Records created: [count]

PROFESSOR OS INTEGRATION:
✅/❌ Professor OS queries this data
✅/❌ Data is used in recommendations
Code references: [list files that use this data]

ISSUES:

- [list any problems found]

RECOMMENDATIONS:

- [actionable steps to fix/improve]
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
═══════════════════════════════════════════════════════════════
PART 2: VIDEO CURATION AUDIT
═══════════════════════════════════════════════════════════════

STEP 1: Check video curation database

Run these queries:

-- Find video/curation tables
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
  AND (
    table_name LIKE '%video%' 
    OR table_name LIKE '%curat%'
    OR table_name LIKE '%technique%'
  );

-- Check yesterday's curation stats
SELECT 
  DATE(created_at) as date,
  COUNT(*) as total_videos,
  COUNT(*) FILTER (WHERE status = 'approved') as approved,
  COUNT(*) FILTER (WHERE status = 'rejected') as rejected,
  COUNT(*) FILTER (WHERE status = 'pending') as pending
FROM curated_videos
WHERE DATE(created_at) >= CURRENT_DATE - INTERVAL '2 days'
GROUP BY DATE(created_at)
ORDER BY date DESC;

-- Check curation runs table if it exists
SELECT 
  DATE(started_at) as date,
  COUNT(*) as total_runs,
  SUM(videos_discovered) as total_discovered,
  SUM(videos_approved) as total_approved,
  SUM(quota_used) as total_quota_used
FROM curation_runs
WHERE DATE(started_at) >= CURRENT_DATE - INTERVAL '2 days'
GROUP BY DATE(started_at)
ORDER BY date DESC;

-- Check today's quota usage
SELECT * FROM api_quota_usage
WHERE date >= CURRENT_DATE - INTERVAL '2 days'
ORDER BY date DESC;

-- Check auto-curation settings
SELECT * FROM system_settings
WHERE setting_key IN (
  'auto_curation_enabled',
  'youtube_api_quota_limit',
  'curation_batch_size',
  'runs_per_day'
);

STEP 2: Check curation cron job

Search for:
- Curation scheduler code
- When it's scheduled to run
- If it's running at maximum capacity
- Any rate limiting or throttling

STEP 3: Calculate maximum capacity

Based on:
- Current YouTube API quota limit
- Number of runs per day
- Batch size per run
- Calculate: Are we using 90% of available quota?

REPORT FORMAT FOR VIDEO CURATION:
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VIDEO CURATION AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

AUTO-CURATION STATUS:
✅/❌ Enabled: [true/false]
YouTube Quota Limit: [10,000 or 1,000,000]
Runs per day: [count]
Batch size: [count]

YESTERDAY’S STATS (Oct 27, 2025):
Curation runs: [count]
Videos discovered: [count]
Videos approved: [count]
Videos rejected: [count]
Quota used: [count] / [limit] ([percentage]%)

TODAY’S STATS (Oct 28, 2025):
Curation runs: [count]
Videos discovered: [count]
Videos approved: [count]
Quota used: [count] / [limit] ([percentage]%)

TOTAL VIDEO LIBRARY:
Total videos in database: [count]
Approved videos: [count]
Quality score average: [score]

MAXIMUM CAPACITY ANALYSIS:
Current daily capacity: [X videos/day]
Theoretical maximum: [Y videos/day]
Utilization: [percentage]%

CRON SCHEDULE:
Runs at: [times, e.g., “2 AM, 10 AM, 6 PM”]
Last run: [timestamp]
Next run: [timestamp]

BOTTLENECKS IDENTIFIED:

- [list any limitations found]

ACCELERATION RECOMMENDATIONS:

- [specific steps to reach maximum speed]
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
═══════════════════════════════════════════════════════════════
PART 3: MAXIMIZE VIDEO CURATION SPEED
═══════════════════════════════════════════════════════════════

Based on findings, implement maximum acceleration:

IF auto_curation_enabled = false:
  → Enable it immediately
  → UPDATE system_settings SET setting_value = 'true' WHERE setting_key = 'auto_curation_enabled';

IF youtube_api_quota_limit = 10000 (free tier):
  → Set runs to maximize quota usage
  → Recommended: 3 runs per day, batch size 300
  → UPDATE system_settings SET setting_value = '300' WHERE setting_key = 'curation_batch_size';
  → UPDATE system_settings SET setting_value = '3' WHERE setting_key = 'runs_per_day';

IF youtube_api_quota_limit = 1000000 (upgraded):
  → Set runs to maximum speed
  → Recommended: 24 runs per day (hourly), batch size 3000
  → UPDATE system_settings SET setting_value = '3000' WHERE setting_key = 'curation_batch_size';
  → UPDATE system_settings SET setting_value = '24' WHERE setting_key = 'runs_per_day';

CHECK cron schedule frequency:
  → If only running 1-2x per day, increase to 3x minimum
  → For upgraded quota, increase to hourly

VERIFY curation is not being throttled:
  → Check for any artificial delays or rate limiting in code
  → Remove unnecessary sleep() or wait() calls
  → Ensure batch processing is efficient

═══════════════════════════════════════════════════════════════
PART 4: COMPREHENSIVE SYSTEM REPORT
═══════════════════════════════════════════════════════════════

After completing all audits, generate a comprehensive report:
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BJJ OS SYSTEM AUDIT REPORT
Generated: [timestamp]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

EXECUTIVE SUMMARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅/❌ Combat sports scraping operational
✅/❌ Video curation operational
✅/❌ Running at maximum capacity
✅/❌ Data being used by Professor OS

COMBAT SPORTS SCRAPING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[Insert Part 1 findings]

VIDEO CURATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[Insert Part 2 findings]

YESTERDAY’S PERFORMANCE (Oct 27, 2025)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Combat sports records: [count]
Videos discovered: [count]
Videos approved: [count]
YouTube quota used: [count] / [limit]

TODAY’S PERFORMANCE (Oct 28, 2025)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Combat sports records: [count]
Videos discovered: [count]
Videos approved: [count]
YouTube quota used: [count] / [limit]

ACCELERATION ACTIONS TAKEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅/❌ Auto-curation enabled
✅/❌ Batch size optimized
✅/❌ Run frequency maximized
✅/❌ Quota usage optimized

CURRENT SYSTEM CAPACITY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Daily video capacity: [X videos/day]
Current utilization: [Y%]
Quota efficiency: [Z%]

ISSUES FOUND
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[List all issues discovered]

RECOMMENDATIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
HIGH PRIORITY:

- [Critical actions needed]

MEDIUM PRIORITY:

- [Optimization opportunities]

LOW PRIORITY:

- [Nice-to-have improvements]

NEXT STEPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. [Immediate action #1]
1. [Immediate action #2]
1. [Immediate action #3]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
═══════════════════════════════════════════════════════════════
PART 5: SAVE REPORT TO FILE
═══════════════════════════════════════════════════════════════

CREATE FILE: /server/system-audit-report-[DATE].txt

Save the complete report to a file with timestamp for future reference.

Also output the report to console so it's visible immediately.

═══════════════════════════════════════════════════════════════
EXECUTION CHECKLIST
═══════════════════════════════════════════════════════════════

Execute in this order:

1. ✅ Search codebase for scraping systems
2. ✅ Query database for today's scraping data
3. ✅ Verify Professor OS integration
4. ✅ Query video curation stats (today + yesterday)
5. ✅ Check auto-curation settings
6. ✅ Calculate maximum capacity
7. ✅ Identify bottlenecks
8. ✅ Implement acceleration changes
9. ✅ Generate comprehensive report
10. ✅ Save report to file
11. ✅ Output to console

═══════════════════════════════════════════════════════════════
SUCCESS CRITERIA
═══════════════════════════════════════════════════════════════

✅ Found combat sports scraping code and data
✅ Verified data from today exists
✅ Confirmed Professor OS uses the data
✅ Retrieved yesterday's video curation stats
✅ Retrieved today's video curation stats
✅ Checked if running at maximum capacity
✅ Identified any bottlenecks or issues
✅ Implemented acceleration optimizations
✅ Generated comprehensive report
✅ Provided actionable recommendations

═══════════════════════════════════════════════════════════════
CRITICAL NOTES
═══════════════════════════════════════════════════════════════

• Be thorough - search entire codebase if needed
• Check multiple potential table names
• If something doesn't exist, clearly state "NOT FOUND"
• Provide specific numbers, not estimates
• Include timestamps for all data points
• Make recommendations actionable and specific
• If quota is at 10K, optimize for that limit
• If quota is at 1M, maximize to that capacity
• Report should be clear and easy to understand
• Save report for future reference

START AUDIT NOW.
```

-----

## ✅ **COMPREHENSIVE AUDIT PROMPT READY!**

**This prompt will:**

1. ✅ Check if combat sports scraping ran today
1. ✅ Show what data was scraped
1. ✅ Verify it’s being saved and used by Professor OS
1. ✅ Show yesterday’s video curation numbers
1. ✅ Show today’s video curation numbers
1. ✅ Check if auto-curation is maxed out
1. ✅ Identify bottlenecks
1. ✅ Implement maximum acceleration
1. ✅ Generate comprehensive report with stats
1. ✅ Provide actionable recommendations

**Copy the entire prompt above and send it to Replit Agent!** 🚀

This will give you a complete picture of:

- What’s working
- What’s not working
- Yesterday’s performance
- Today’s performance
- How to maximize speed

The report will be saved and also output to console so you can see it immediately!​​​​​​​​​​​​​​​​