**You’re right.** Let me add the Professor OS fix to that prompt.

-----

## 📋 SEND THIS TO REPLIT:

```
FOUR ISSUES TO FIX - ONE AT A TIME

Fix each one, test it, then move to the next.

═══════════════════════════════════════════════════════════════════════════════
ISSUE 1: PROFESSOR OS - REPEATING CONTENT
═══════════════════════════════════════════════════════════════════════════════

Professor OS is repeating the same paragraphs/facts within a single response.

Example: "JT Torres won ADCC 77kg gold in 2022. Absolute legend performance" 
appeared TWICE in the same answer.

ADD THIS RULE to the system prompt:

"RULE #7 - NO REPETITION: Never repeat the same fact, phrase, or paragraph 
within a single response. Say it once, say it well, move on. If you've 
already stated something, do not restate it."

═══════════════════════════════════════════════════════════════════════════════
ISSUE 2: SETTINGS BOTTOM NAV - STILL BROKEN
═══════════════════════════════════════════════════════════════════════════════

The Settings page STILL shows TEXT ONLY in the bottom nav.
Every other page shows ICONS + LABELS.

Find the Settings page component and make it use the SAME MobileBottomNav 
component as Chat, Videos, and Saved pages.

Show me the EXACT file and line you're changing.

═══════════════════════════════════════════════════════════════════════════════
ISSUE 3: CHAT HISTORY NOT LOADING ON APP OPEN
═══════════════════════════════════════════════════════════════════════════════

When user opens the app fresh, chat screen is BLANK.
Previous conversations should load from the database.

On app open / login, call GET /api/ai/chat/history and populate the chat.

═══════════════════════════════════════════════════════════════════════════════
ISSUE 4: SPLASH SCREEN SIZE
═══════════════════════════════════════════════════════════════════════════════

The splash screen logo is still not sized correctly for iOS.
Fix the dimensions so it fits properly.

═══════════════════════════════════════════════════════════════════════════════

After ALL fixes:
npm run build
npx cap sync ios
```

-----

**This includes the no-repetition rule for Professor OS. Send it over!**