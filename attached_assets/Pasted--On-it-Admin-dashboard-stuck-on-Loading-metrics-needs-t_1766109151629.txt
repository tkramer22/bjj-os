**On it.** Admin dashboard stuck on “Loading metrics…” needs to be fixed.

-----

## 📋 SEND THIS TO REPLIT AGENT:

```
ADMIN DASHBOARD FIX - STUCK ON "LOADING METRICS..."

The admin dashboard at bjjos.app/admin/dashboard is stuck showing "Loading metrics..." and never loads.

═══════════════════════════════════════════════════════════════════════════════
1. DIAGNOSE THE ISSUE
═══════════════════════════════════════════════════════════════════════════════

Check the following:

A) Backend API endpoint:
- What endpoint does the dashboard call for metrics?
- Is that endpoint returning data or erroring?
- Check server logs for errors when dashboard loads

B) Frontend console:
- Are there any JavaScript errors in browser console?
- Is the API call timing out?
- Is there a CORS issue?

C) Database query:
- What query fetches dashboard metrics?
- Is it timing out? (check for slow queries)
- Run the query directly and see if it returns data

Show me:
- The API endpoint code for dashboard metrics
- Any errors in the logs
- The database query being run

═══════════════════════════════════════════════════════════════════════════════
2. FIX THE LOADING ISSUE
═══════════════════════════════════════════════════════════════════════════════

Once diagnosed, fix the root cause:
- If API error: Fix the endpoint
- If timeout: Optimize the query or add indexes
- If frontend error: Fix the component
- If no data: Ensure metrics are being calculated/stored

═══════════════════════════════════════════════════════════════════════════════
3. VERIFY ALL DASHBOARD METRICS WORK
═══════════════════════════════════════════════════════════════════════════════

After fixing the loading issue, confirm these metrics display correctly:

- Total users
- Active subscribers  
- Trial users
- Total videos in library
- Videos pending Gemini processing
- Daily/weekly/monthly active users
- Revenue metrics (if shown)
- Any other metrics on the dashboard

═══════════════════════════════════════════════════════════════════════════════
4. ADD ERROR HANDLING
═══════════════════════════════════════════════════════════════════════════════

If metrics fail to load, dashboard should:
- Show error message instead of infinite "Loading..."
- Include retry button
- Log the error for debugging

Don't let it hang forever on "Loading metrics..."

═══════════════════════════════════════════════════════════════════════════════
5. TEST
═══════════════════════════════════════════════════════════════════════════════

After fixes:
- Reload bjjos.app/admin/dashboard
- Confirm metrics load within 2-3 seconds
- Verify all numbers are accurate
- Check no console errors

Show me the root cause and confirm the fix.
```

-----

This will get Replit to diagnose why it’s stuck and fix it properly with error handling so it doesn’t happen again.

Send it?​​​​​​​​​​​​​​​​