# 🎯 **SMART CURATION - FIND THE GAPS**

You’re 100% right. Searching the same categories wastes quota. Let’s find what’s missing first.

-----

## **📋 COPY TO AGENT**

```
SMART CURATION - Find gaps, then fill them.

═══════════════════════════════════════════════════════════════
STEP 1: ANALYZE CURRENT LIBRARY - FIND THE GAPS
═══════════════════════════════════════════════════════════════

Run these queries and show me the results:

1. Videos by POSITION (what's underserved?):
SELECT position_category, COUNT(*) as count 
FROM ai_video_knowledge 
GROUP BY position_category 
ORDER BY count ASC;

2. Videos by TECHNIQUE TYPE (attack vs defense ratio):
SELECT technique_type, COUNT(*) as count 
FROM ai_video_knowledge 
GROUP BY technique_type;

3. Videos by INSTRUCTOR (who's missing?):
SELECT instructor_name, COUNT(*) as count 
FROM ai_video_knowledge 
GROUP BY instructor_name 
ORDER BY count DESC 
LIMIT 30;

4. Recent search queries already run (if tracked):
SELECT search_query, COUNT(*) as times_run, MAX(created_at) as last_run
FROM curation_logs 
GROUP BY search_query 
ORDER BY times_run DESC 
LIMIT 30;

5. What searches have we NOT done yet?

═══════════════════════════════════════════════════════════════
STEP 2: IDENTIFY GAPS
═══════════════════════════════════════════════════════════════

Based on the data above, tell me:

1. Which POSITIONS have fewer than 30 videos? (gaps)
2. Which elite INSTRUCTORS have fewer than 10 videos?
3. What's our attack:defense ratio? (we likely need more defense)
4. Which TECHNIQUES are underrepresented?

═══════════════════════════════════════════════════════════════
STEP 3: BUILD NEW QUERY LIST (avoid duplicates)
═══════════════════════════════════════════════════════════════

Create a fresh query list targeting ONLY:
- Positions with < 30 videos
- Instructors with < 10 videos
- Defense techniques (if ratio is off)
- Specific techniques we're missing

DO NOT repeat queries we've already run multiple times.

═══════════════════════════════════════════════════════════════
STEP 4: RUN TARGETED CURATION
═══════════════════════════════════════════════════════════════

Run curation with the NEW gap-filling queries only.
Quality threshold: 8.0+
Tag everything properly.

Show me what you're searching BEFORE you run it so I can approve.

═══════════════════════════════════════════════════════════════

First: Show me the gap analysis (Steps 1-2).
Then: Show me the proposed new query list (Step 3).
Then: I'll approve and you run it (Step 4).
```

-----

This way we:

- ✅ Don’t waste quota on duplicate searches
- ✅ Fill actual gaps in the library
- ✅ Maximize search-to-save ratio
- ✅ Build a more complete library

**Send it and let’s see where the holes are.** 🎯​​​​​​​​​​​​​​​​