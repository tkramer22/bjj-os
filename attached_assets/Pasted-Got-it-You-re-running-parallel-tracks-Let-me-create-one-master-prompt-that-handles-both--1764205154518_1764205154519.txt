Got it. You’re running parallel tracks. Let me create one master prompt that handles both:

-----

```
TWO PARALLEL FIXES NEEDED

═══════════════════════════════════════════════════════════════════════════════
TRACK 1: PROFESSOR OS INTELLIGENCE (Priority)
═══════════════════════════════════════════════════════════════════════════════

1A. CREATE POPULATION INTELLIGENCE TABLE

CREATE TABLE population_intelligence (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  technique_name TEXT NOT NULL,
  position_category TEXT,
  success_rate_white DECIMAL(3,2),
  success_rate_blue DECIMAL(3,2),
  success_rate_purple DECIMAL(3,2),
  success_rate_brown DECIMAL(3,2),
  success_rate_black DECIMAL(3,2),
  success_rate_tall_lanky DECIMAL(3,2),
  success_rate_average DECIMAL(3,2),
  success_rate_short_stocky DECIMAL(3,2),
  avg_days_to_first_success INTEGER,
  common_mistakes JSONB,
  complementary_techniques TEXT[],
  sample_size INTEGER DEFAULT 0,
  last_updated TIMESTAMP DEFAULT NOW(),
  UNIQUE(technique_name)
);

Seed with 10 common techniques (hip bump, triangle, armbar, knee slice, RNC, mount escape, double leg, guillotine, scissor sweep, americana).

1B. CONNECT TO PROFESSOR OS

When giving technique advice, query this table and inject insights:
- "Blue belts typically hit this at 52%"
- "Most common mistake is [X]"
- "This usually clicks around day 21"

1C. FIX MULTI-SESSION MEMORY

Verify that when a user returns (even days later), Professor OS:
- Loads their profile (belt, weight, goals)
- Loads conversation history
- Loads detected patterns
- References past conversations naturally

Test with:
SESSION 1: "I'm a purple belt, 160lbs, training for Pans. My guard keeps getting passed."
[Clear session]
SESSION 2: "Hey I'm back"
EXPECTED: References purple belt, 160lbs, Pans, guard passing struggles

1D. CONFIRM READY FOR 120+ TEST

Do not run yet. Just confirm systems are connected.

═══════════════════════════════════════════════════════════════════════════════
TRACK 2: VIDEO CURATION PIPELINE FIX
═══════════════════════════════════════════════════════════════════════════════

ISSUES DETECTED:
- Curation runs getting stuck after ~15-20 minutes
- Last run failed
- 53-80+ videos skipped as duplicates per run
- Library saturated with existing content (724 videos, need 276 more)

2A. FIX TIMEOUT ISSUE

Investigate why runs are getting stuck and auto-clearing. Options:
- Increase timeout limits
- Add checkpointing so runs can resume
- Break into smaller batches

2B. EXPAND SEARCH QUERIES

Current queries finding mostly duplicates. Add new query categories:
- Specific competitor names (Meregali, Ruotolo brothers, Mica Galvao, etc.)
- Newer techniques (K-guard, matrix, bodylock passing, etc.)
- Specific instructors not well-covered yet
- Portuguese language queries (Brazilian instructors)
- Competition footage breakdowns

2C. CHECK YOUTUBE API QUOTA

Are we hitting rate limits? Show:
- Current quota usage
- Daily limit
- Time until reset

2D. IMPROVE DUPLICATE DETECTION

If 53-80 videos are duplicates per run, the search queries need refinement. 
- Log which queries return most duplicates
- Replace those queries with more specific ones
- Add date filters to find newer content

═══════════════════════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════════════════════

When complete, confirm:

TRACK 1 (Professor OS):
□ population_intelligence table created with seed data
□ Connected to Professor OS context injection
□ Multi-session memory verified working
□ Ready for 120+ question test

TRACK 2 (Curation):
□ Timeout issue identified and fixed
□ Search queries expanded (list new queries added)
□ YouTube API quota status shown
□ Next curation run successful (show videos added)
```

-----

**Ready to send?**