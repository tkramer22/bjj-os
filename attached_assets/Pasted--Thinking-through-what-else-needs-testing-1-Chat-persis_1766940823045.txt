**Thinking through what else needs testing:**

1. ✅ Chat persistence (tabs + logout/login)
1. ✅ Chat scroll behavior (recent messages, bottom)
1. ✅ Chat duplicate message bug fixed
1. ✅ Video library count (2,357)
1. ✅ Dropdown filters with real counts
1. ✅ Profile showing real user data
1. ✅ Saved videos loading

**Additional items to test:**

- **Video playback** - Do videos actually play when tapped?
- **Save/bookmark a video** - Does the bookmark button work?
- **Search functionality** - Does search work in library?
- **Bottom navigation** - All 4 tabs working with icons + labels?
- **Logout flow** - Does Sign Out work properly?
- **Chat input** - Can you type and send a new message?
- **Voice input** - Does microphone button work?
- **Settings links** - Do Terms, Privacy, Help links work?
- **Manage Subscription** - Does the button work?

-----

**Here’s the master prompt:**

```
COMPLETE iOS APP TESTING - ALL FEATURES

Test everything in the browser at bjjos.app BEFORE I push to iOS simulator.

═══════════════════════════════════════════════════════════════════════════════
PAGE 1: CHAT (/ios-chat)
═══════════════════════════════════════════════════════════════════════════════

DISPLAY:
- Does it scroll to BOTTOM (most recent messages visible)?
- Is text rendering correctly (no overlap/garbled text)?
- How many messages load initially?
- Is the bottom navigation bar visible with 4 tabs?

FUNCTIONALITY:
- Type a test message and send - does it work?
- Does the AI response appear ONCE (not duplicated)?
- Does the microphone/voice input button work?

PERSISTENCE TEST:
- Send a message
- Click to Videos tab
- Click back to Chat tab
- Is the conversation still there? (must NOT reset)
- Log out completely
- Log back in
- Is full chat history still there?

═══════════════════════════════════════════════════════════════════════════════
PAGE 2: VIDEO LIBRARY (/ios-library)
═══════════════════════════════════════════════════════════════════════════════

DISPLAY:
- Confirm dropdown shows "All (2,357)" - exact count
- Click Techniques dropdown - LIST all options with their counts
- Click Professors dropdown - LIST all options with their counts
- Are video thumbnails loading?
- Does each video show: title, instructor, duration?

FUNCTIONALITY:
- Does search bar work? (type "guard" - do results filter?)
- Click on a video - does it play?
- Click bookmark icon on a video - does it save?
- Does infinite scroll/pagination work for more videos?

═══════════════════════════════════════════════════════════════════════════════
PAGE 3: SAVED VIDEOS (/ios-saved)
═══════════════════════════════════════════════════════════════════════════════

DISPLAY:
- Does it show the user's saved/bookmarked videos?
- What count does it show? (e.g., "12 videos saved")
- Do dropdowns have real counts?

FUNCTIONALITY:
- If you bookmarked a video in library, does it appear here?
- Does search work?
- Can you remove a saved video?

═══════════════════════════════════════════════════════════════════════════════
PAGE 4: PROFILE/SETTINGS (/ios-profile)
═══════════════════════════════════════════════════════════════════════════════

DISPLAY:
- What NAME shows? (should be actual user name, not "BJJ Practitioner")
- What EMAIL shows? (should be actual email, not "No email")
- What SUBSCRIPTION shows? (should be "Lifetime", not "Free")

FUNCTIONALITY:
- Does "Manage Subscription" button work?
- Does "Settings" link work (or open correctly)?
- Does "Terms of Service" link work?
- Does "Privacy Policy" link work?
- Does "Help & Support" link work?
- Does "Sign Out" button work? (logs out and returns to login screen)

═══════════════════════════════════════════════════════════════════════════════
BOTTOM NAVIGATION
═══════════════════════════════════════════════════════════════════════════════

- Are all 4 tabs visible: Chat, Videos, Saved, Settings?
- Does each tab have an ICON + LABEL?
- Does tapping each tab navigate to correct page?
- Is the active tab highlighted?
- Is it fixed at bottom on ALL pages?

═══════════════════════════════════════════════════════════════════════════════
AUTHENTICATION
═══════════════════════════════════════════════════════════════════════════════

- Log out from Profile page
- Does it return to login screen?
- Log back in with email/password
- Does it redirect to Chat?
- Are all pages accessible after login?
- Does "Forgot Password" flow work?

═══════════════════════════════════════════════════════════════════════════════
REPORT FORMAT
═══════════════════════════════════════════════════════════════════════════════

For each section above, report:
✅ PASS - working correctly
❌ FAIL - describe what's wrong
⚠️ PARTIAL - works but has issues

Do NOT say "should work" - actually test each item and report results.

I need confirmation of every feature before pushing to iOS simulator.
```