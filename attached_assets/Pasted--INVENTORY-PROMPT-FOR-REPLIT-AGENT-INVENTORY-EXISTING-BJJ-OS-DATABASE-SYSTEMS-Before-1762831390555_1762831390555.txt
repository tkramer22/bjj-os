# **📋 INVENTORY PROMPT FOR REPLIT AGENT**

```
INVENTORY EXISTING BJJ OS DATABASE & SYSTEMS

Before building Professor OS learning system, I need a complete inventory of existing infrastructure.

═══════════════════════════════════════════════════════════════
TASK 1: DATABASE SCHEMA INVENTORY
═══════════════════════════════════════════════════════════════

Show me ALL database tables and their structures:

```sql
-- 1. List all tables
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;

-- 2. For each table found, show full structure:
-- (Run this for EACH table)
SELECT 
  column_name,
  data_type,
  character_maximum_length,
  is_nullable,
  column_default
FROM information_schema.columns
WHERE table_name = 'TABLE_NAME_HERE'
ORDER BY ordinal_position;
```

CRITICAL TABLES TO PRIORITIZE:

- User tables (bjjUsers, users, profiles, etc.)
- Chat/message tables (aiChatHistory, messages, conversations, etc.)
- Video tables (curatedVideos, videos, etc.)
- Any insights/analytics tables
- Any scraping/external data tables
- Any learning/tracking tables

For EACH table, show:
✅ Table name
✅ All columns with types
✅ Primary keys
✅ Foreign keys
✅ Indexes
✅ Example row (if data exists)

═══════════════════════════════════════════════════════════════
TASK 2: CONVERSATION HISTORY SYSTEM
═══════════════════════════════════════════════════════════════

Find and show:

1. Where are Professor OS conversations stored?

- Table name?
- Schema?
- How many messages currently stored?

1. How is conversation history loaded?
   
   ```typescript
   // Find the code that loads chat history
   // Show me the query/function
   ```
1. Is conversation history passed to Professor OS?

- Where in the code?
- How many messages included?

1. Show example conversation record:
   
   ```sql
   SELECT * FROM [chat_table] 
   WHERE user_id = [any_user_id]
   ORDER BY created_at DESC 
   LIMIT 5;
   ```

═══════════════════════════════════════════════════════════════
TASK 3: VIDEO LIBRARY SYSTEM
═══════════════════════════════════════════════════════════════

Show:

1. Video table structure:
   
   ```sql
   DESCRIBE curatedVideos;
   -- or equivalent command to show structure
   ```
1. Current video count:
   
   ```sql
   SELECT COUNT(*) FROM curatedVideos;
   ```
1. Video fields available:

- Title, instructor, technique type?
- Belt level, duration?
- Thumbnail, URL?
- Any relevance scoring?

1. Example video records:
   
   ```sql
   SELECT * FROM curatedVideos LIMIT 3;
   ```

═══════════════════════════════════════════════════════════════
TASK 4: EXISTING SCRAPING SYSTEM
═══════════════════════════════════════════════════════════════

Todd mentioned “system is already supposedly scraping combat sports news and data”

Find and document:

1. Search codebase for scraping-related code:

- File locations with “scrape”, “fetch”, “external”, “news”
- Any cron jobs or scheduled tasks
- Any YouTube/API integrations

1. What data is being scraped?

- YouTube videos?
- News articles?
- Competition results?
- Instructor content?

1. Where is scraped data stored?

- Table name?
- Schema?
- How many records?

1. How is scraped data accessed?

- Is it used by Professor OS currently?
- How would we read it?

Show me the code and table structure.

═══════════════════════════════════════════════════════════════
TASK 5: EXISTING ANALYTICS/INSIGHTS
═══════════════════════════════════════════════════════════════

Check for any existing analytics or insights tracking:

1. Are there tables tracking:

- User patterns?
- Common struggles?
- Video effectiveness?
- Training frequency?

1. Any existing learning/ML systems?
1. Any metadata being tracked on conversations?

═══════════════════════════════════════════════════════════════
TASK 6: USER PROFILE STRUCTURE
═══════════════════════════════════════════════════════════════

Show complete user profile schema:

```sql
-- Assuming bjjUsers table
SELECT * FROM bjjUsers LIMIT 1;
```

Fields available:

- Personal info (name, email, etc.)?
- BJJ data (belt, style, training frequency)?
- Physical stats (height, weight, body type)?
- Goals, struggles, injuries?
- Any preference/settings fields?

═══════════════════════════════════════════════════════════════
OUTPUT FORMAT
═══════════════════════════════════════════════════════════════

For each section, provide:

1. **Table/System Name**
1. **Schema/Structure** (full details)
1. **Current data volume** (row count)
1. **Example records** (sanitize sensitive data)
1. **Code locations** (file paths where used)
1. **Integration notes** (how Professor OS currently uses it, if at all)

═══════════════════════════════════════════════════════════════

GOAL: Understand EVERYTHING that exists so we can integrate the learning system WITHOUT breaking existing functionality.

Show me comprehensive output for ALL of the above tasks.

```
---

**🚀 Copy that entire prompt and send it to Replit Agent.**

Once we see what exists, I'll design the integration strategy that:
- ✅ Preserves all existing functionality
- ✅ Adds learning capability on top
- ✅ Uses existing scraping infrastructure
- ✅ Integrates with current chat/video systems

**Send it and show me what Agent returns!** 💪​​​​​​​​​​​​​​​​
```