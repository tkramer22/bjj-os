Two problems here — the curation pipeline is running too many times and finding almost nothing each run, then emailing you for every single one.
Paste this into Replit:

FIX CURATION PIPELINE — TOO MANY EMAILS, TOO FEW VIDEOS

Two issues:

1. I'm getting 8+ emails at once from the curation system. 
   Most say "0 videos added" or "+1 video". This is spam.

2. The pipeline is finding very few videos per run ("low yield 
   warning" on every single one).

══════════════════════════════════════════════════════════════════════════
FIX 1: EMAIL NOTIFICATIONS — ONE DAILY SUMMARY ONLY
══════════════════════════════════════════════════════════════════════════

STOP sending an email for every single curation run.

Instead, send ONE daily summary email at the end of all runs.

The daily summary should include:
- Total videos analyzed across all runs
- Total videos added across all runs
- Total videos rejected (and top reasons)
- Next scheduled run time

Example:
  Subject: "📊 Daily Curation Report: +7 videos"
  Body: "Today's curation analyzed 45 videos across 8 categories. 
         7 new videos added to the library. 38 rejected 
         (duplicates: 20, too short: 10, low quality: 8).
         Next run: Tomorrow at 12:00 PM EST."

If ZERO videos were added all day, still send one email but say:
  Subject: "📊 Daily Curation Report: No new videos"
  
DO NOT send individual emails per run. Batch them into one daily email.

══════════════════════════════════════════════════════════════════════════
FIX 2: LOW YIELD — DIAGNOSE WHY
══════════════════════════════════════════════════════════════════════════

The "low yield warning" on every run means the pipeline is either:
a) Searching the same queries repeatedly and finding duplicates
b) Filtering too aggressively
c) Running out of new content for current search terms

Check:
1. What search queries is the pipeline using?
2. Are they rotating or is it the same queries every run?
3. How many total videos are already in the database?
4. What percentage of found videos are being rejected as duplicates?
5. Show me the rejection breakdown: how many rejected for each reason

If most rejections are duplicates, the pipeline needs NEW search 
queries or different YouTube channels to pull from.

══════════════════════════════════════════════════════════════════════════
FIX 3: REDUCE RUN FREQUENCY
══════════════════════════════════════════════════════════════════════════

How often is the curation pipeline currently scheduled? If it's 
running 8 times per day with low yield, reduce to 2-3 runs per day 
max. More runs with zero results is just wasting API calls.

Show me the current cron schedule and recommend a better one.

══════════════════════════════════════════════════════════════════════════
SHOW ME
══════════════════════════════════════════════════════════════════════════

1. Current cron schedule for curation
2. Current search queries being used
3. Total videos in database right now
4. Rejection breakdown from today's runs
5. The email sending code — where individual emails are triggered


The main fix is consolidating those 8 emails into one daily digest. No one needs 8 “0 videos added” emails. Let me know what Replit finds on the yield issue.​​​​​​​​​​​​​​​​