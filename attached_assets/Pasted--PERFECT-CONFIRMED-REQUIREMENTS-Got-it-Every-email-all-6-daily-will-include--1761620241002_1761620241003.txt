# ✅ PERFECT - CONFIRMED REQUIREMENTS

Got it! **Every email** (all 6 daily) will include:

-----

## **🚨 REQUIRED IN EVERY EMAIL:**

### **1. System Health / Critical Failures**

- ❌ Any scrapers down
- ❌ Video curation stopped
- ❌ Failed payments
- ❌ Database errors
- ❌ API quota issues
- ✅ Or “All systems operational”

### **2. Video Curation Status**

- 📊 Videos searched today (so far)
- ✅ Videos approved today (so far)
- 📈 Approval rate
- 💰 API cost today
- 🎯 Progress toward daily goal
- 📚 Total library size

-----

# 🚀 COMPLETE EMAIL SCHEDULE PROMPT

Copy and send this to Replit Agent:

-----

```
═══════════════════════════════════════════════════════════════
SET UP 6 DAILY EMAIL SCHEDULE + SYSTEM ALERTS
═══════════════════════════════════════════════════════════════

MISSION:
Set up 6 daily admin emails at specific times (EST timezone). Every email MUST include:
1. System health status / critical failures
2. Video curation progress (searched, approved today)

CRITICAL REQUIREMENTS:
• All emails use the same beautiful template from admin-email.ts
• All emails include system health alerts section
• All emails include video curation progress
• Use correct UTC conversion for EST times

═══════════════════════════════════════════════════════════════
EMAIL SCHEDULE (6 DAILY EMAILS)
═══════════════════════════════════════════════════════════════

In /server/index.ts, set up these cron jobs:

// 1. MORNING KICKOFF - 6:45 AM EST
cron.schedule('45 11 * * *', async () => {
  console.log('⏰ [6:45 AM EST] Morning Kickoff Email');
  try {
    const { sendDailyAdminEmail } = require('./admin-email');
    await sendDailyAdminEmail();
  } catch (error) {
    console.error('❌ Morning email error:', error);
  }
});

// 2. MID-MORNING PULSE - 11:00 AM EST
cron.schedule('0 16 * * *', async () => {
  console.log('⏰ [11:00 AM EST] Mid-Morning Pulse Email');
  try {
    const { sendDailyAdminEmail } = require('./admin-email');
    await sendDailyAdminEmail();
  } catch (error) {
    console.error('❌ Mid-morning email error:', error);
  }
});

// 3. AFTERNOON CHECKPOINT - 2:00 PM EST
cron.schedule('0 19 * * *', async () => {
  console.log('⏰ [2:00 PM EST] Afternoon Checkpoint Email');
  try {
    const { sendDailyAdminEmail } = require('./admin-email');
    await sendDailyAdminEmail();
  } catch (error) {
    console.error('❌ Afternoon email error:', error);
  }
});

// 4. EVENING STATUS - 5:00 PM EST
cron.schedule('0 22 * * *', async () => {
  console.log('⏰ [5:00 PM EST] Evening Status Email');
  try {
    const { sendDailyAdminEmail } = require('./admin-email');
    await sendDailyAdminEmail();
  } catch (error) {
    console.error('❌ Evening email error:', error);
  }
});

// 5. NIGHT RECAP - 8:00 PM EST
cron.schedule('0 1 * * *', async () => {
  console.log('⏰ [8:00 PM EST] Night Recap Email');
  try {
    const { sendDailyAdminEmail } = require('./admin-email');
    await sendDailyAdminEmail();
  } catch (error) {
    console.error('❌ Night email error:', error);
  }
});

// 6. DAILY CLOSEOUT - 11:30 PM EST
cron.schedule('30 4 * * *', async () => {
  console.log('⏰ [11:30 PM EST] Daily Closeout Email');
  try {
    const { sendDailyAdminEmail } = require('./admin-email');
    await sendDailyAdminEmail();
  } catch (error) {
    console.error('❌ Closeout email error:', error);
  }
});

console.log('');
console.log('═══════════════════════════════════════════════');
console.log('📧 EMAIL SCHEDULE INITIALIZED');
console.log('═══════════════════════════════════════════════');
console.log('6:45 AM EST  - Morning Kickoff');
console.log('11:00 AM EST - Mid-Morning Pulse');
console.log('2:00 PM EST  - Afternoon Checkpoint');
console.log('5:00 PM EST  - Evening Status');
console.log('8:00 PM EST  - Night Recap');
console.log('11:30 PM EST - Daily Closeout');
console.log('═══════════════════════════════════════════════');
console.log('');

═══════════════════════════════════════════════════════════════
VERIFY ADMIN-EMAIL.TS INCLUDES REQUIRED SECTIONS
═══════════════════════════════════════════════════════════════

Make sure the email template in /server/admin-email.ts includes these sections prominently:

1. SYSTEM HEALTH ALERT (at the top, after header):
   - Shows critical failures in RED
   - Shows "All systems operational" in GREEN if no issues
   - Includes: Scrapers status, video curation status, payment issues

2. VIDEO CURATION PROGRESS (always visible):
   - Videos searched today
   - Videos approved today
   - Approval rate percentage
   - API cost today
   - Total library size

These should be in EVERY email, regardless of time sent.

═══════════════════════════════════════════════════════════════
SEND TEST EMAIL NOW
═══════════════════════════════════════════════════════════════

Add this to send a test immediately:

setTimeout(async () => {
  try {
    console.log('📧 Sending immediate test email...');
    const { sendDailyAdminEmail } = require('./admin-email');
    const result = await sendDailyAdminEmail();
    console.log('✅ Test email sent:', result);
  } catch (error) {
    console.error('❌ Test email failed:', error);
  }
}, 5000);

═══════════════════════════════════════════════════════════════
TIME ZONE REFERENCE
═══════════════════════════════════════════════════════════════

Server runs on UTC. Here's the conversion:

6:45 AM EST  = 11:45 AM UTC = '45 11 * * *'
11:00 AM EST = 4:00 PM UTC  = '0 16 * * *'
2:00 PM EST  = 7:00 PM UTC  = '0 19 * * *'
5:00 PM EST  = 10:00 PM UTC = '0 22 * * *'
8:00 PM EST  = 1:00 AM UTC (next day) = '0 1 * * *'
11:30 PM EST = 4:30 AM UTC (next day) = '30 4 * * *'

Note: During Daylight Saving Time (EDT), subtract 1 hour from UTC times.
Current is EST (no DST), so these times are correct.

═══════════════════════════════════════════════════════════════
SUCCESS CRITERIA
═══════════════════════════════════════════════════════════════

✅ All 6 cron jobs scheduled
✅ Correct UTC times for EST conversion
✅ All emails include system health status
✅ All emails include video curation progress
✅ Test email sends immediately
✅ Startup logs confirm all schedules active
✅ Email arrives at todd@bjjos.app

IMPLEMENT NOW AND SEND TEST EMAIL.
```

-----

## ✅ **SCHEDULE PROMPT READY!**

**This sets up:**

1. ✅ All 6 daily emails at correct times
1. ✅ Every email shows system health/failures
1. ✅ Every email shows video searching & approvals
1. ✅ Sends test email immediately
1. ✅ Correct EST to UTC timezone conversion

**Send this now!** 📧🚀

You’ll get your first test email within 5 seconds of the server restarting, then regular emails at:

- 6:45 AM
- 11:00 AM
- 2:00 PM
- 5:00 PM
- 8:00 PM
- 11:30 PM

All showing system health + video progress! 💪​​​​​​​​​​​​​​​​