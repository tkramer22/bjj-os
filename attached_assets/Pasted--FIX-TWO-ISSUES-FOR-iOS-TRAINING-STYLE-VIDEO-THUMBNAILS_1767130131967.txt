```
FIX TWO ISSUES FOR iOS - TRAINING STYLE & VIDEO THUMBNAILS

═══════════════════════════════════════════════════════════════════════════════
ISSUE 1: TRAINING STYLE - ADD "BOTH" OPTION + CAROUSEL UI
═══════════════════════════════════════════════════════════════════════════════

THIS IS FOR iOS (ios-profile, ios components)

Currently the Style selector only shows:
- Gi
- No-Gi

There is NO "Both" option. This needs to be added.

ALSO: Change the Style selector from a dropdown to a CAROUSEL (scroll picker) 
just like Height and Weight already use.

Requirements:
1. Add "Both" as a third option
2. Convert Style selector to carousel/scroll picker UI (same component used for Height and Weight)
3. Options in carousel: Gi | No-Gi | Both

Do the same for BELT RANK:
- Convert Belt selector to carousel/scroll picker UI
- Same style as Height and Weight
- Options: White | Blue | Purple | Brown | Black

Files to update:
- ios-profile components
- Any iOS-specific profile/settings screens
- Ensure the carousel component matches the Height/Weight picker exactly

═══════════════════════════════════════════════════════════════════════════════
ISSUE 2: VIDEO THUMBNAIL FALLBACK - NOT WORKING ON iOS
═══════════════════════════════════════════════════════════════════════════════

When Professor OS recommends a video in chat on iOS, the thumbnail area shows:
- Blank/white space
- Just a play button
- NO image

The fallback to BJJ OS logo is NOT working on iOS.

Debug and fix:
1. Check if YouTube thumbnail URL is being passed to the iOS component
2. Check if the image is failing to load (CORS, URL format, etc.)
3. Ensure the BJJ OS logo fallback triggers when thumbnail fails
4. Test with actual video recommendations in iOS simulator

The thumbnail should show:
- YouTube thumbnail if available
- BJJ OS logo if thumbnail fails to load
- NEVER a blank white/gray space

Check these iOS-specific files:
- ThumbnailImage.tsx (iOS version)
- Any chat message components that render video cards
- Ensure onError fallback is implemented for the image

═══════════════════════════════════════════════════════════════════════════════
TESTING (iOS SIMULATOR)
═══════════════════════════════════════════════════════════════════════════════

After fixes, test in iOS simulator:

1. STYLE CAROUSEL: Profile > Style
   - Should be a scroll carousel (not dropdown)
   - Should show: Gi | No-Gi | Both
   
2. BELT CAROUSEL: Profile > Belt
   - Should be a scroll carousel (not dropdown)
   - Should show: White | Blue | Purple | Brown | Black

3. VIDEO THUMBNAIL: Ask Professor OS "show me a guard passing video"
   - Should see YouTube thumbnail OR BJJ OS logo
   - Should NOT see blank space

All changes are for iOS components specifically.
═══════════════════════════════════════════════════════════════════════════════
```