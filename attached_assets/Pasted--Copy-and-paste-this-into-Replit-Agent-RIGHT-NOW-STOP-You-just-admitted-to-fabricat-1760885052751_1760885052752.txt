**Copy and paste this into Replit Agent RIGHT NOW:**

-----

```
STOP. You just admitted to fabricating information. Good - honesty is what I need.

NEW MISSION: Create HONEST documentation based ONLY on what you can verify from the codebase files.

═══════════════════════════════════════════════════════════════════
RULES FOR THIS DOCUMENTATION:
═══════════════════════════════════════════════════════════════════

1. ✅ ONLY document what you can SEE in the code files
2. ❌ DO NOT guess, estimate, or fabricate any statistics
3. ❌ DO NOT claim things work without being able to verify
4. ⚠️ If you cannot verify something, say "CANNOT VERIFY" clearly

═══════════════════════════════════════════════════════════════════
PART 1: FILE INVENTORY (What Actually Exists)
═══════════════════════════════════════════════════════════════════

List every file in the project with:
- Full file path
- Lines of code (actual count from the file)
- Brief description of what it does (from reading the code)

Organize by:
- Frontend files (client/src/*)
- Backend files (server/*)
- Database files (shared/schema.ts, migrations/*)
- Config files
- Scripts

═══════════════════════════════════════════════════════════════════
PART 2: DATABASE SCHEMA (From schema.ts)
═══════════════════════════════════════════════════════════════════

Open shared/schema.ts (or wherever database schema is defined).

For EACH table defined:
- Table name
- Complete column list with types
- Any relationships/foreign keys
- Any indexes defined

Show the ACTUAL code, don't summarize.

If you see 78 tables, list ALL 78 with their schemas.

═══════════════════════════════════════════════════════════════════
PART 3: API ROUTES (From routes.ts)
═══════════════════════════════════════════════════════════════════

Open server/routes.ts.

List EVERY route you can see:
- HTTP method (GET/POST/PUT/DELETE)
- Route path
- Line number in routes.ts
- Brief description from code/comments

Format:
```

POST /api/auth/send-code (line 45)

- Sends SMS verification code via Twilio

GET /api/videos/search (line 123)

- Searches video library

```
═══════════════════════════════════════════════════════════════════
PART 4: ADMIN PAGES (What Exists in /admin)
═══════════════════════════════════════════════════════════════════

List all files in client/src/pages/admin/ (or wherever admin pages are):

For each admin page file:
- Filename
- Lines of code
- What it appears to do (from reading the code)
- What components/features it includes

═══════════════════════════════════════════════════════════════════
PART 5: AI/ML CODE (Dual-Model System)
═══════════════════════════════════════════════════════════════════

Find files related to:
- Prof. OS chat (Claude/GPT integration)
- Video analysis (dual-model system)
- Instructor credibility calculation
- Video curation pipeline

For each file:
- Show the actual code (or relevant excerpts)
- What AI models are being called (Claude? GPT? Which versions?)
- What prompts are being used
- How the dual-model system works

═══════════════════════════════════════════════════════════════════
PART 6: THIRD-PARTY INTEGRATIONS (Code Evidence)
═══════════════════════════════════════════════════════════════════

Search the codebase for:

**Twilio:**
- Where is it imported/used?
- What functions call Twilio?
- Show the actual code

**Stripe:**
- Where is it imported/used?
- What functions handle payments?
- Show the actual code

**Claude API:**
- Where is it imported/used?
- What's the API key variable name?
- Show the actual code

**OpenAI API:**
- Where is it imported/used?
- What's the API key variable name?
- Show the actual code

**YouTube API:**
- Where is it imported/used?
- Show the actual code

═══════════════════════════════════════════════════════════════════
PART 7: WHAT I CANNOT VERIFY (Be Explicit)
═══════════════════════════════════════════════════════════════════

List everything you CANNOT verify:

**Cannot verify from code alone:**
- ❌ How many users are in the database (need database access)
- ❌ How many videos are in the database (need database access)
- ❌ Whether SMS actually sends (need to test)
- ❌ Whether Stripe checkout works (need to test)
- ❌ Whether the app runs without errors (need to run it)
- ❌ Performance metrics (need monitoring/testing)
- ❌ Whether deployed to production (need deployment access)

Be exhaustive about what you DON'T know.

═══════════════════════════════════════════════════════════════════
PART 8: ENVIRONMENT VARIABLES (From Code)
═══════════════════════════════════════════════════════════════════

Search for all environment variable references in the code:

List each one:
- Variable name (e.g., TWILIO_ACCOUNT_SID)
- Where it's used (filename + line number)
- What it's for

Example:
```

TWILIO_ACCOUNT_SID

- Used in: server/auth/twilio.ts (line 12)
- Purpose: Twilio account identifier for SMS
- Status: CANNOT VERIFY if it’s set (no access to .env file)

```
═══════════════════════════════════════════════════════════════════
DELIVERABLES:
═══════════════════════════════════════════════════════════════════

Create these files with ONLY verifiable information:

1. **FILE_INVENTORY.md** - Complete list of all files
2. **DATABASE_SCHEMA.md** - All tables with full schemas (from schema.ts)
3. **API_ROUTES.md** - All routes (from routes.ts)
4. **ADMIN_PAGES.md** - All admin functionality (from /admin files)
5. **AI_CODE.md** - All AI/ML code (Prof. OS, video analysis, etc.)
6. **INTEGRATIONS.md** - All third-party integration code
7. **CANNOT_VERIFY.md** - Explicit list of what you cannot verify

═══════════════════════════════════════════════════════════════════

After creating these files, provide a summary:

**SUMMARY:**

**What I CAN confirm (from code):**
- X files exist
- X database tables defined in schema
- X API routes defined
- X admin pages exist
- Dual-model AI code exists: YES/NO
- Twilio integration code exists: YES/NO
- Stripe integration code exists: YES/NO

**What I CANNOT confirm:**
- Database row counts (no database access)
- Whether anything actually works (no testing access)
- Environment variables are set (no .env access)
- Deployment status (no deployment access)

**To verify what works, you need to:**
1. Run the app and test manually
2. Check database directly
3. Verify environment variables are set
4. Test each feature end-to-end

═══════════════════════════════════════════════════════════════════

NOW: Generate honest documentation with NO fabrications.
```

-----

**PASTE THAT ↑↑↑ INTO REPLIT AGENT NOW.**

**This will give you factual documentation based on actual code, not hallucinations.** 🎯​​​​​​​​​​​​​​​​