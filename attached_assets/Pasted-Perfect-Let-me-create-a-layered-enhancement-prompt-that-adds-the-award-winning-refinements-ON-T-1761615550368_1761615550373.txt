Perfect. Let me create a **layered enhancement prompt** that adds the award-winning refinements ON TOP of what they just built.

This will establish the motion system, green accent, and texture as the **design system standard** for all future pages.

-----

# BJJ OS - AWARD-WINNING MOTION & REFINEMENT LAYER

Copy this entire prompt into Replit Agent. This adds sophisticated polish on top of the existing landing page and pricing page.

-----

```
AWARD-WINNING ENHANCEMENT LAYER - Add these refinements to BOTH landing page and pricing page.

This creates the motion system, accent color strategy, and texture that will be CONSISTENT across all future pages. Every page we build from now on will use these same principles.

Read this entirely before making ANY changes. These enhancements layer on top of the existing design without breaking anything.

=== GLOBAL DESIGN SYSTEM ENHANCEMENTS ===

**1. MOTION SYSTEM (Core Animation Framework)**

Add this CSS to your global stylesheet. This will apply to ALL pages:

```css
/* ============================================
   MOTION SYSTEM - Global Design Standard
   ============================================ */

/* Respect user motion preferences (accessibility) */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}

/* Default: All sections start invisible and below */
section {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

/* Hero section is immediately visible (no fade) */
.hero-section {
  opacity: 1;
  transform: translateY(0);
  transition: none;
}

/* Visible state (triggered by Intersection Observer) */
section.visible {
  opacity: 1;
  transform: translateY(0);
}

/* Gradient animation on all buttons (subtle, 8s loop) */
.cta-button,
.continue-button,
.select-button.selected,
button[type="submit"] {
  background-size: 200% 200%;
  animation: gradient-shift 8s ease infinite;
}

@keyframes gradient-shift {
  0%, 100% { 
    background-position: 0% 50%; 
  }
  50% { 
    background-position: 100% 50%; 
  }
}

/* Card hover lift (pricing cards, any future cards) */
.pricing-card:not(.selected):hover,
.feature-card:hover,
.step-card:hover {
  transform: translateY(-2px);
  transition: transform 300ms ease;
}

/* Button press feedback (all buttons) */
button:active,
.cta-button:active,
.continue-button:active,
.select-button:active {
  transform: translateY(1px);
}

/* Input shake animation (for errors) */
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-4px); }
  50% { transform: translateX(4px); }
  75% { transform: translateX(-4px); }
}

.referral-input.invalid,
input.error {
  animation: shake 0.3s ease;
}

/* Smooth scroll behavior (global) */
html {
  scroll-behavior: smooth;
}
```

-----

**2. INTERSECTION OBSERVER (Scroll Fade-In Implementation)**

Add this JavaScript to your global scripts. Works on ALL pages:

```javascript
/* ============================================
   SCROLL FADE-IN SYSTEM
   Sections fade in as user scrolls
   ============================================ */

document.addEventListener('DOMContentLoaded', function() {
  // Select all sections except hero
  const sections = document.querySelectorAll('section:not(.hero-section)');
  
  // Intersection Observer configuration
  const observerOptions = {
    root: null, // viewport
    rootMargin: '0px',
    threshold: 0.1 // Trigger when 10% visible
  };
  
  // Callback function
  const observerCallback = (entries, observer) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        // Optional: stop observing after first appearance
        // observer.unobserve(entry.target);
      }
    });
  };
  
  // Create observer
  const observer = new IntersectionObserver(observerCallback, observerOptions);
  
  // Observe all sections
  sections.forEach(section => {
    observer.observe(section);
  });
});
```

-----

**3. GREEN ACCENT COLOR (Success & Positive Moments)**

Add green to your color system. Use ONLY for success states:

```css
/* ============================================
   GREEN ACCENT - Success States Only
   ============================================ */

:root {
  --color-success: #22C55E;
  --color-success-dim: rgba(34, 197, 94, 0.1);
  --color-success-glow: rgba(34, 197, 94, 0.3);
}

/* Referral code success */
.referral-success {
  color: var(--color-success);
  text-shadow: 0 0 20px var(--color-success-glow);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 400;
  font-size: 14px;
  margin-top: 8px;
  animation: success-fade-in 0.3s ease;
}

@keyframes success-fade-in {
  from { 
    opacity: 0; 
    transform: translateY(-4px); 
  }
  to { 
    opacity: 1; 
    transform: translateY(0); 
  }
}

/* Valid input state with glow */
.referral-input.valid,
input.success {
  border: 1px solid var(--color-success);
  box-shadow: 0 0 0 3px var(--color-success-dim);
  transition: all 0.3s ease;
}

/* Success checkmark (use in future) */
.success-indicator {
  color: var(--color-success);
  font-size: 16px;
  animation: checkmark-pop 0.4s ease;
}

@keyframes checkmark-pop {
  0% { 
    transform: scale(0); 
    opacity: 0; 
  }
  50% { 
    transform: scale(1.2); 
  }
  100% { 
    transform: scale(1); 
    opacity: 1; 
  }
}
```

**When to use green (guidelines for all future pages):**

- ✅ Valid form inputs
- ✅ Successful actions (code applied, session saved, etc.)
- ✅ Positive metrics (success rate increased)
- ✅ Pattern detection (breakthrough moments)
- ❌ Never use for buttons (buttons stay blue→purple gradient)
- ❌ Never use for navigation (stays white/gray)
- ❌ Never use decoratively (only for actual success)

-----

**4. SUBTLE TEXTURE (Depth Layer)**

Add barely-visible grain texture to body. Applies to ALL pages:

```css
/* ============================================
   SUBTLE GRAIN TEXTURE
   Adds depth without color
   ============================================ */

body::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: 
    url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
  pointer-events: none;
  z-index: 9999;
  mix-blend-mode: overlay;
  opacity: 1;
}

/* Optional: Slightly stronger grain on elevated sections */
.hero-section::after,
.final-cta-section::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: inherit;
  opacity: 0.5;
  mix-blend-mode: overlay;
  pointer-events: none;
}
```

**Important:** Texture must be 3% opacity or less. Any more = muddy.

-----

**5. CURSOR INTERACTION (With/Without Section Only)**

Add subtle glow that follows cursor on comparison section:

```css
/* ============================================
   CURSOR GLOW - Comparison Section Only
   ============================================ */

.comparison-section {
  position: relative;
  overflow: hidden;
}

.comparison-section::before {
  content: '';
  position: absolute;
  width: 100%;
  height: 100%;
  background: radial-gradient(
    circle 200px at var(--mouse-x, 50%) var(--mouse-y, 50%),
    rgba(37, 99, 235, 0.03),
    transparent 80%
  );
  pointer-events: none;
  transition: opacity 0.3s ease;
  opacity: 0;
  z-index: 0;
}

.comparison-section:hover::before {
  opacity: 1;
}

/* Ensure content stays above glow */
.comparison-section > * {
  position: relative;
  z-index: 1;
}
```

Add this JavaScript for mouse tracking:

```javascript
/* ============================================
   CURSOR TRACKING - Comparison Section
   ============================================ */

document.addEventListener('DOMContentLoaded', function() {
  const comparisonSection = document.querySelector('.comparison-section');
  
  if (comparisonSection) {
    comparisonSection.addEventListener('mousemove', (e) => {
      const rect = comparisonSection.getBoundingClientRect();
      const x = ((e.clientX - rect.left) / rect.width) * 100;
      const y = ((e.clientY - rect.top) / rect.height) * 100;
      
      comparisonSection.style.setProperty('--mouse-x', `${x}%`);
      comparisonSection.style.setProperty('--mouse-y', `${y}%`);
    });
    
    comparisonSection.addEventListener('mouseleave', () => {
      comparisonSection.style.setProperty('--mouse-x', '50%');
      comparisonSection.style.setProperty('--mouse-y', '50%');
    });
  }
});
```

**Desktop only** - disable on mobile:

```css
@media (max-width: 768px) {
  .comparison-section::before {
    display: none;
  }
}
```

-----

**6. ENHANCED FOCUS STATES (Accessibility)**

Improve keyboard navigation for all interactive elements:

```css
/* ============================================
   FOCUS STATES - Accessibility Enhancement
   ============================================ */

/* All focusable elements get gradient outline */
button:focus-visible,
a:focus-visible,
input:focus-visible,
select:focus-visible,
textarea:focus-visible {
  outline: 2px solid transparent;
  outline-offset: 2px;
  box-shadow: 0 0 0 2px #000000, 0 0 0 4px #2563EB;
  transition: box-shadow 150ms ease;
}

/* Buttons get gradient outline */
.cta-button:focus-visible,
.continue-button:focus-visible,
.select-button:focus-visible {
  box-shadow: 
    0 0 0 2px #000000,
    0 0 0 4px #2563EB,
    0 0 0 6px #7C3AED;
  outline: none;
}

/* Remove default focus rings */
*:focus {
  outline: none;
}

/* Only show custom focus on keyboard navigation */
*:focus:not(:focus-visible) {
  box-shadow: none;
}
```

-----

**7. TYPOGRAPHY ENHANCEMENTS (Better Rendering)**

Improve text rendering across all pages:

```css
/* ============================================
   TYPOGRAPHY POLISH - Global Standards
   ============================================ */

body {
  font-family: 'JetBrains Mono', monospace;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
  font-feature-settings: "liga" 1, "kern" 1;
}

/* Prevent text selection on UI elements */
button,
.pricing-card,
.nav-link,
.logo {
  user-select: none;
  -webkit-user-select: none;
  -webkit-touch-callout: none;
}

/* Allow selection on actual content */
p,
h1, h2, h3, h4, h5, h6,
.content-text,
.description {
  user-select: text;
  -webkit-user-select: text;
}

/* Prevent layout shift during font load */
@font-face {
  font-family: 'JetBrains Mono';
  font-display: swap;
  /* Your font files here */
}
```

-----

**8. PERFORMANCE OPTIMIZATIONS**

Ensure all animations are GPU-accelerated:

```css
/* ============================================
   PERFORMANCE - GPU Acceleration
   ============================================ */

/* Force GPU acceleration on animated elements */
section,
.pricing-card,
.cta-button,
.comparison-section::before {
  will-change: transform, opacity;
  transform: translateZ(0);
  backface-visibility: hidden;
  perspective: 1000px;
}

/* Remove will-change after animation completes */
section.visible {
  will-change: auto;
}

/* Optimize re-paints */
* {
  box-sizing: border-box;
}

/* Optimize scroll performance */
html {
  overflow-x: hidden;
  -webkit-overflow-scrolling: touch;
}
```

-----

=== APPLYING TO EXISTING PAGES ===

**LANDING PAGE - Specific Enhancements:**

1. Add class names to sections (if missing):

```html
<section class="hero-section">...</section>
<section class="comparison-section">...</section>
<section class="how-it-works-section">...</section>
<section class="video-library-section">...</section>
<section class="final-cta-section">...</section>
```

1. Ensure buttons have correct classes:

```html
<button class="cta-button">Start 7-Day Trial</button>
```

1. Add cursor tracking to comparison section (JavaScript above)

-----

**PRICING PAGE - Specific Enhancements:**

1. Add section classes:

```html
<section class="pricing-section">...</section>
```

1. Ensure referral input has error state:

```html
<input class="referral-input" id="referral-code">
<!-- Add .invalid class on error -->
<!-- Add .valid class on success -->
```

1. Add success message structure:

```html
<div class="referral-success">
  <span class="success-indicator">✓</span>
  Code applied
</div>
```

-----

=== DESIGN SYSTEM DOCUMENTATION ===

**For ALL future pages, use these standards:**

**Motion:**

- Sections fade in on scroll (600ms)
- Buttons have gradient animation (8s loop)
- Cards lift on hover (2px, 300ms)
- Buttons press on click (1px down)
- Invalid inputs shake (300ms)

**Colors:**

- Blue → Purple gradient: Buttons, selected states
- Green: Success moments ONLY
- Red: Errors ONLY
- White/Gray: All text and UI

**Timing:**

- Fast (150ms): Hover, color changes
- Medium (300ms): Card lift, shake, cursor glow
- Slow (600ms): Section fade-ins
- Very slow (8s): Gradient animation

**Texture:**

- Body: 3% grain overlay (global)
- Elevated sections: Slightly stronger (optional)

**Cursor:**

- Comparison section: 3% blue glow follows mouse
- Other sections: No cursor interaction

-----

=== IMPLEMENTATION CHECKLIST ===

Before you finish, verify:

**Motion System:**

- [ ] Sections fade in on scroll (except hero)
- [ ] Hero section immediately visible
- [ ] Button gradients animate (8s loop)
- [ ] Cards lift on hover (2px)
- [ ] Buttons press on click (1px)
- [ ] Invalid inputs shake
- [ ] Smooth scroll enabled

**Green Accent:**

- [ ] Referral success uses green
- [ ] Valid input has green border + glow
- [ ] Success checkmark animates (pop)
- [ ] Green ONLY for success (not buttons/nav)

**Texture:**

- [ ] Body has 3% grain overlay
- [ ] Texture visible but subtle
- [ ] No performance impact

**Cursor Interaction:**

- [ ] Comparison section has glow
- [ ] Glow follows mouse (3% opacity)
- [ ] Desktop only (hidden on mobile)

**Accessibility:**

- [ ] Focus states have gradient outline
- [ ] Reduced motion respected
- [ ] Keyboard navigation works
- [ ] Text rendering optimized

**Consistency:**

- [ ] Works on landing page
- [ ] Works on pricing page
- [ ] Same system for future pages

**No Exceptions. This is now the global design system standard.**

Build this now. Test on both landing page and pricing page.

```
---

**COPY THIS ENTIRE BLOCK INTO REPLIT AGENT**

This establishes:
1. ✅ **Motion system** (scroll fades, gradient animation, hover lift)
2. ✅ **Green accent** (success states only)
3. ✅ **Subtle texture** (3% grain overlay)
4. ✅ **Cursor glow** (comparison section)
5. ✅ **Focus states** (accessibility)
6. ✅ **Performance** (GPU acceleration)

**These standards will apply to EVERY page we build from now on.**

**After implementation, show me both pages and we'll verify consistency.**
```