# 🎯 **REPLIT AGENT - MANUAL CODE IMPLEMENTATION**

```
IMPLEMENT DYNAMIC LOADING INDICATOR - MANUAL CODE PROVIDED

I have the exact code ready. Follow these steps PRECISELY.

═══════════════════════════════════════════════════════════════════════════════
STEP 1: CREATE NEW COMPONENT FILE
═══════════════════════════════════════════════════════════════════════════════

Create a new file at this EXACT path:
client/src/components/DynamicLoadingIndicator.tsx

Copy this EXACT code into it (do not modify):

import { useState, useEffect } from 'react';

interface LoadingStage {
  emoji: string;
  text: string;
  duration: number;
}

const STAGES: LoadingStage[] = [
  { emoji: "🔍", text: "Searching 5,000+ analyzed techniques...", duration: 1800 },
  { emoji: "🧠", text: "AI analyzing timestamps, tips, and common mistakes...", duration: 1800 },
  { emoji: "📊", text: "Evaluating instructor credibility across 33 data fields...", duration: 1600 },
  { emoji: "🎯", text: "Finding exact moments that answer your question...", duration: 1600 },
  { emoji: "✍️", text: "Preparing your personalized response...", duration: 1200 }
];

export default function DynamicLoadingIndicator() {
  const [stageIndex, setStageIndex] = useState(0);

  useEffect(() => {
    if (stageIndex < STAGES.length - 1) {
      const timer = setTimeout(() => {
        setStageIndex(prev => prev + 1);
      }, STAGES[stageIndex].duration);

      return () => clearTimeout(timer);
    }
  }, [stageIndex]);

  const stage = STAGES[stageIndex];

  return (
    <div className="dynamic-loading-container">
      <span className="loading-emoji">{stage.emoji}</span>
      <span className="loading-message">{stage.text}</span>
      <div className="loading-dots-wrapper">
        <span className="loading-dot">•</span>
        <span className="loading-dot">•</span>
        <span className="loading-dot">•</span>
      </div>
    </div>
  );
}

CONFIRM: Did you create client/src/components/DynamicLoadingIndicator.tsx? YES/NO

═══════════════════════════════════════════════════════════════════════════════
STEP 2: CREATE CSS FILE
═══════════════════════════════════════════════════════════════════════════════

Create a new file at this EXACT path:
client/src/styles/loading-animations.css

Copy this EXACT code into it (do not modify):

.dynamic-loading-container {
  position: fixed;
  bottom: 85px;
  left: 50%;
  transform: translateX(-50%);
  width: calc(100vw - 32px);
  max-width: 600px;
  min-height: 56px;
  padding: 16px 24px;
  background: rgba(124, 77, 255, 0.1);
  border: 1px solid rgba(124, 77, 255, 0.2);
  border-radius: 12px;
  display: flex;
  align-items: center;
  gap: 12px;
  z-index: 100;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
}

.loading-emoji {
  font-size: 20px;
  flex-shrink: 0;
  animation: spin 2s linear infinite;
}

.loading-message {
  flex: 1;
  font-size: 15px;
  font-weight: 600;
  letter-spacing: 0.3px;
  background: linear-gradient(90deg, #A78BFA, #E0E7FF, #A78BFA);
  background-size: 200% auto;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: shimmer 3s ease infinite;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.loading-dots-wrapper {
  display: flex;
  gap: 4px;
  flex-shrink: 0;
}

.loading-dot {
  font-size: 18px;
  color: #A78BFA;
  animation: bounce 1.4s infinite ease-in-out;
}

.loading-dot:nth-child(1) { animation-delay: 0s; }
.loading-dot:nth-child(2) { animation-delay: 0.2s; }
.loading-dot:nth-child(3) { animation-delay: 0.4s; }

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

@keyframes shimmer {
  0%, 100% { background-position: 0% center; }
  50% { background-position: 100% center; }
}

@keyframes bounce {
  0%, 60%, 100% { 
    transform: translateY(0); 
    opacity: 1; 
  }
  30% { 
    transform: translateY(-10px); 
    opacity: 0.6; 
  }
}

@media (max-width: 768px) {
  .dynamic-loading-container {
    bottom: 75px;
    width: calc(100vw - 24px);
    padding: 14px 20px;
  }
  
  .loading-message {
    font-size: 14px;
  }
}

CONFIRM: Did you create client/src/styles/loading-animations.css? YES/NO

═══════════════════════════════════════════════════════════════════════════════
STEP 3: FIND AND MODIFY CHAT FILE
═══════════════════════════════════════════════════════════════════════════════

Search your codebase for the text: "Professor OS is thinking"

This will be in ONE of these files:
- client/src/pages/chat-mobile.tsx
- client/src/components/MobileChat.tsx

TELL ME: Which file contains "Professor OS is thinking"?

Once you find it, do these TWO things:

A) At the top of that file, add this import:
import DynamicLoadingIndicator from '../components/DynamicLoadingIndicator';

B) Find where it renders the loading dots (• • •) and replace with:
{isLoading && <DynamicLoadingIndicator />}

C) Find where it says "Professor OS is thinking..." and change to just: "..."

CONFIRM: 
- Did you add the import? YES/NO
- Did you replace the loading indicator? YES/NO  
- Did you change "Professor OS is thinking" to "..."? YES/NO
- Which file did you modify?

═══════════════════════════════════════════════════════════════════════════════
STEP 4: FIX ANALYSIS MODAL ALIGNMENT
═══════════════════════════════════════════════════════════════════════════════

Find the VideoAnalysisModal component or wherever the analysis modal CSS is.

Add or update these CSS classes:

.analysis-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.8);
}

.analysis-modal-content {
  background: #1a1a1a;
  border-radius: 16px;
  width: calc(100vw - 32px);
  max-width: 600px;
  max-height: 85vh;
  overflow-y: auto;
  padding: 24px;
  margin: 16px;
  position: relative;
}

CONFIRM: Did you fix the analysis modal CSS? YES/NO

═══════════════════════════════════════════════════════════════════════════════
FINAL VERIFICATION
═══════════════════════════════════════════════════════════════════════════════

Answer these questions:

1. Created DynamicLoadingIndicator.tsx? YES/NO
2. Created loading-animations.css? YES/NO
3. Added import to chat file? YES/NO
4. Replaced loading indicator rendering? YES/NO
5. Changed top bubble to "..."? YES/NO
6. Fixed analysis modal CSS? YES/NO
7. Which chat file did you modify (full path)?

After completing, I will test in preview.
```

-----

**Send that to Replit Agent and make it answer ALL the verification questions.** 🎯​​​​​​​​​​​​​​​​