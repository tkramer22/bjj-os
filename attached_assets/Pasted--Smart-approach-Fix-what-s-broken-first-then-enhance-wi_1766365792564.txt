**Smart approach.** Fix what’s broken first, then enhance without breaking anything.

Let me give you a prompt that’s **additive only** - no changes to existing functionality.

-----

## 📋 COPY THIS - BRANDING + PREMIUM FEATURES (ADDITIVE ONLY):

```
iOS APP - BRANDING & PREMIUM ENHANCEMENTS

IMPORTANT: These are ADDITIVE changes only. Do NOT modify any existing 
functionality. The core app is working - we're just polishing it.

═══════════════════════════════════════════════════════════════════════════════
1. SPLASH SCREEN LOGO
═══════════════════════════════════════════════════════════════════════════════

Update the splash screen to show the actual BJJ OS logo.

The logo is the ||| BJJ OS mark (three bars + text).

Update:
- ios/App/App/Assets.xcassets/Splash.imageset/
- Make it centered, clean, on dark background

═══════════════════════════════════════════════════════════════════════════════
2. iOS HOME SCREEN APP ICON
═══════════════════════════════════════════════════════════════════════════════

Replace the text-only icon with the actual BJJ OS logo icon.

Update ios/App/App/Assets.xcassets/AppIcon.appiconset/ with proper sizes:
- 1024x1024 (App Store)
- 180x180 (iPhone @3x)
- 120x120 (iPhone @2x)
- 167x167 (iPad Pro)
- 152x152 (iPad @2x)
- 87x87, 80x80, 76x76, 60x60, 58x58, 40x40, 29x29, 20x20

Use the BJJ OS logo mark, not just text.

═══════════════════════════════════════════════════════════════════════════════
3. HAPTIC FEEDBACK (ADDITIVE)
═══════════════════════════════════════════════════════════════════════════════

Install Capacitor Haptics:
npm install @capacitor/haptics
npx cap sync ios

Add subtle haptic feedback to:
- Button taps (Light impact)
- Saving a video (Success notification)
- Sending a chat message (Light impact)
- Tab bar navigation (Light impact)

Example usage:
import { Haptics, ImpactStyle } from '@capacitor/haptics';

// On button tap
await Haptics.impact({ style: ImpactStyle.Light });

// On success action
await Haptics.notification({ type: 'success' });

DO NOT change button functionality - just ADD haptic feedback to existing handlers.

═══════════════════════════════════════════════════════════════════════════════
4. PULL-TO-REFRESH (ADDITIVE)
═══════════════════════════════════════════════════════════════════════════════

Add pull-to-refresh gesture to:
- Chat page (refreshes/reloads chat history)
- Video Library (refreshes video list)
- Saved Videos (refreshes saved list)

This is additive - just wrap existing content in a pull-to-refresh container.

═══════════════════════════════════════════════════════════════════════════════
5. iOS-STYLE LOADING SPINNER
═══════════════════════════════════════════════════════════════════════════════

Replace any generic loading indicators with iOS-style spinner.

Use a simple CSS spinner that matches iOS aesthetic:
- Thin lines
- Smooth animation
- Subtle, not distracting

═══════════════════════════════════════════════════════════════════════════════
CRITICAL RULES:
═══════════════════════════════════════════════════════════════════════════════

- DO NOT modify existing API calls
- DO NOT change navigation logic
- DO NOT alter video player functionality
- DO NOT touch authentication flow
- ONLY ADD enhancements to existing working code

These are polish features - the core app must remain unchanged.

═══════════════════════════════════════════════════════════════════════════════
AFTER CHANGES:
═══════════════════════════════════════════════════════════════════════════════

npm run build
npx cap sync ios

Test that ALL existing functionality still works:
1. Login still works
2. Chat still works
3. Videos still play
4. Navigation still works
5. Settings still works

Then verify new features:
- Splash screen shows logo
- App icon shows logo
- Haptics work on taps (requires real device)
- Pull-to-refresh works
```

-----

**Send this after Replit finishes the bug fixes. The “CRITICAL RULES” section should prevent them from breaking anything.**

**You’re right that haptics can only be tested on a real device, not the simulator. But the code will be in place for when you do test on device or submit to App Store.**