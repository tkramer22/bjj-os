Perfect. Let’s make this award-winning.

-----

## **AWARD-WINNING DESIGN PRINCIPLES WE’RE APPLYING**

**What wins design awards:**

1. **Restraint** - Most designers overdo it. We’ll do less, better.
1. **Precision** - Every pixel intentional. No approximations.
1. **Contrast** - Bold hierarchy. Clear focal points.
1. **Rhythm** - Consistent spacing creates flow.
1. **Sophistication** - Subtle gradients, not loud ones.
1. **Confidence** - No explaining, no hedging. Direct.

-----

# BJJ OS LANDING PAGE - AWARD-WINNING CORRECTION PROMPT

Copy this entire prompt into Replit Agent to fix the current implementation.

-----

```
CRITICAL DESIGN CORRECTIONS - Read this entirely before making ANY changes.

You built a landing page that's 70% correct. These corrections will make it award-winning. Every change listed here is MANDATORY and PRECISE. No approximations.

=== GLOBAL FIXES ===

**1. BUTTON GRADIENT (CRITICAL - WRONG COLOR)**

Current state: Solid bright blue (incorrect)
Required state: Blue-to-purple gradient (sophisticated)

ALL buttons on the page:
```css
background: linear-gradient(135deg, #2563EB 0%, #7C3AED 100%);
border: none;
box-shadow: none;

/* Hover state */
&:hover {
  background: linear-gradient(135deg, #1D4ED8 0%, #6D28D9 100%);
  /* Slightly darker version of same gradient */
}

/* Active state */
&:active {
  background: linear-gradient(135deg, #1E40AF 0%, #5B21B6 100%);
}
```

This applies to:

- Nav “Sign Up” button
- Hero “Start 7-Day Trial” button
- Final CTA “Start 7-Day Trial” button

-----

**2. LOGO COLOR (CRITICAL - WRONG)**

Current state: Gold/tan colored
Required state: Pure white

```css
.logo-mark {
  /* The || BJJ OS mark */
  color: #FFFFFF;
  /* If using background on elements: */
  background: #FFFFFF;
}

.logo-text {
  /* BJJ OS text */
  color: #FFFFFF;
}
```

NO gold, NO tan, NO gray. Pure white only.

-----

**3. ELEVATED BACKGROUNDS (MISSING - CRITICAL)**

Current state: Everything is #000000
Required state: Hero and Final CTA have subtle lift

```css
/* Default page background */
body {
  background: #000000;
}

/* Hero section ONLY */
.hero-section {
  background: #0F0F0F; /* Barely lighter than black */
  /* This is 15/255 = 6% lighter */
  /* Should be BARELY visible, subliminal */
}

/* Final CTA section ONLY */
.final-cta-section {
  background: #0F0F0F; /* Same as hero - bookends */
}

/* All other sections */
.comparison-section,
.how-it-works-section,
.video-library-section {
  background: #000000; /* Pure black void */
}
```

The #0F0F0F lift should be BARELY detectable. If it’s obvious, you did it wrong.

-----

**4. SPACING SYSTEM (COMPRESSED - FIX)**

Current state: Everything too tight
Required state: Generous breathing room

**Hero section:**

```css
.hero-section {
  padding-top: 120px; /* NOT 80px */
  padding-bottom: 120px; /* NOT 80px */
  padding-left: 40px;
  padding-right: 40px;
}

.hero-headline {
  margin-bottom: 32px; /* Space before paragraph 1 */
}

.hero-paragraph-1 {
  margin-bottom: 24px; /* Space before paragraph 2 */
}

.hero-paragraph-2 {
  margin-bottom: 64px; /* Space before CTA button */
}

.hero-cta-button {
  margin-bottom: 16px; /* Space before trust line */
}

.hero-trust-line {
  margin-bottom: 0; /* End of hero section */
}
```

**Between sections (all):**

```css
/* Vertical space between major sections */
section + section {
  margin-top: 80px; /* Minimum, some need more */
}

/* Hero to Comparison */
.hero-section + .comparison-section {
  margin-top: 80px;
}

/* Comparison to How It Works */
.comparison-section + .how-it-works-section {
  margin-top: 80px;
}

/* How It Works to Video Library */
.how-it-works-section + .video-library-section {
  margin-top: 80px;
}

/* Video Library to Final CTA */
.video-library-section + .final-cta-section {
  margin-top: 80px;
}
```

ALL spacing must be multiples of 8px. No exceptions.

-----

**5. WITH/WITHOUT SECTION (BROKEN - FIX)**

Current state: No divider, weak contrast, wrong placement
Required state: Clear structure, strong contrast

```css
.comparison-section {
  background: #000000; /* Pure black, NOT elevated */
  padding: 80px 40px;
  max-width: 1100px;
  margin: 0 auto;
  position: relative;
}

.comparison-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 80px;
  position: relative;
}

/* Vertical divider line between columns (desktop only) */
.comparison-grid::before {
  content: '';
  position: absolute;
  left: 50%;
  top: 80px; /* Below headers */
  bottom: 0;
  width: 1px;
  background: #1A1A1A; /* Subtle dark gray */
  transform: translateX(-50%);
}

/* Column headers */
.comparison-header {
  font-size: 14px;
  font-weight: 700;
  color: #71717A; /* Dim gray */
  text-transform: uppercase;
  letter-spacing: 0.1em;
  margin-bottom: 48px;
}

/* Left column (without) - DIMMER */
.without-item {
  font-size: 18px;
  color: #71717A; /* Dim - less important */
  line-height: 1.6;
  margin-bottom: 32px;
}

/* Arrow */
.comparison-arrow {
  font-size: 20px;
  color: #2563EB; /* Blue accent */
  margin: 0 16px;
}

/* Right column (with) - BRIGHTER */
.with-item {
  font-size: 18px;
  color: #FFFFFF; /* White - more important */
  line-height: 1.6;
  margin-bottom: 32px;
}

/* Mobile: remove divider, stack vertically */
@media (max-width: 768px) {
  .comparison-grid {
    grid-template-columns: 1fr;
    gap: 48px;
  }
  
  .comparison-grid::before {
    display: none; /* No vertical line on mobile */
  }
  
  /* Add horizontal divider between sections */
  .without-column::after {
    content: '';
    display: block;
    width: 100%;
    height: 1px;
    background: #1A1A1A;
    margin: 48px 0;
  }
}
```

CRITICAL: The contrast between left (dim) and right (bright) MUST be obvious.

-----

**6. HOW IT WORKS SECTION (WEAK STRUCTURE - FIX)**

Current state: Everything floats, no containment
Required state: Clear structure, strong hierarchy

```css
.how-it-works-section {
  background: #000000; /* Pure black void */
  padding: 80px 40px;
  max-width: 1100px;
  margin: 0 auto;
}

.section-header {
  font-size: 32px;
  font-weight: 700;
  color: #FFFFFF;
  text-align: center;
  margin-bottom: 48px; /* NOT 32px */
  letter-spacing: 0.05em;
}

.steps-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
}

/* Individual step card */
.step-card {
  background: transparent; /* NO visible card */
  padding: 32px;
  text-align: center;
}

.step-number {
  font-size: 48px; /* Large */
  font-weight: 700;
  color: #FFFFFF;
  line-height: 1;
  margin-bottom: 16px;
}

.step-title {
  font-size: 20px; /* NOT 16px */
  font-weight: 700;
  color: #FFFFFF;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 8px;
  line-height: 1.2;
}

.step-subtitle {
  font-size: 16px;
  font-weight: 400;
  color: #71717A;
  margin-bottom: 16px;
}

.step-description {
  font-size: 16px; /* NOT 14px */
  font-weight: 400;
  color: #A0A0A0;
  line-height: 1.6;
  max-width: 280px;
  margin: 0 auto;
}

/* Mobile */
@media (max-width: 768px) {
  .steps-grid {
    grid-template-columns: 1fr;
    gap: 48px;
  }
}
```

-----

**7. VIDEO LIBRARY SECTION (NEEDS DISTINCTION)**

Current state: Generic section
Required state: Clear, focused

```css
.video-library-section {
  background: #000000;
  padding: 64px 40px; /* Slightly less than major sections */
  max-width: 700px; /* Narrower than other sections */
  margin: 0 auto;
  text-align: center;
}

.video-library-header {
  font-size: 32px;
  font-weight: 700;
  color: #FFFFFF;
  margin-bottom: 24px;
  letter-spacing: 0.02em;
}

.video-library-description {
  font-size: 18px;
  font-weight: 400;
  color: #A0A0A0;
  line-height: 1.6;
}
```

-----

**8. FINAL CTA SECTION (NEEDS ELEVATED BACKGROUND)**

Current state: Floats in void
Required state: Elevated bookend (matches hero)

```css
.final-cta-section {
  background: #0F0F0F; /* ELEVATED - same as hero */
  padding: 80px 40px;
  max-width: 600px;
  margin: 0 auto;
  text-align: center;
}

.final-cta-headline {
  font-size: 48px;
  font-weight: 700;
  color: #FFFFFF;
  line-height: 1.2;
  margin-bottom: 8px;
}

.final-cta-subheadline {
  font-size: 16px;
  font-weight: 400;
  color: #A0A0A0;
  margin-bottom: 48px;
  line-height: 1.6;
}

.final-cta-button {
  /* Uses global button gradient from Fix #1 */
  margin-bottom: 24px;
}

.final-cta-footer-text {
  font-size: 14px;
  font-weight: 400;
  color: #71717A;
}
```

-----

**9. FOOTER (TOO PROMINENT - FIX)**

Current state: Links too bright, draws too much attention
Required state: Subtle, professional

```css
footer {
  background: #000000;
  padding: 48px 24px;
  text-align: center;
}

.footer-links {
  display: flex;
  flex-direction: column;
  gap: 24px;
  margin-bottom: 32px;
}

.footer-link {
  font-size: 16px;
  font-weight: 400;
  color: #A0A0A0; /* NOT white, NOT bright */
  text-decoration: none;
  padding: 12px 0;
  transition: color 150ms ease;
}

.footer-link:hover {
  color: #FFFFFF;
}

.coming-soon-label {
  font-size: 12px;
  color: #71717A; /* Even dimmer */
  font-weight: 400;
  letter-spacing: 0.05em;
}

.footer-copyright {
  font-size: 14px;
  color: #71717A;
  border-top: 1px solid #1A1A1A;
  padding-top: 32px;
  margin-top: 32px;
}

/* Desktop */
@media (min-width: 768px) {
  .footer-links {
    flex-direction: row;
    justify-content: center;
    gap: 32px;
    margin-bottom: 16px;
  }
  
  .footer-link:not(:last-child)::after {
    content: '•';
    margin-left: 32px;
    color: #1A1A1A;
  }
  
  .footer-copyright {
    border-top: none;
    padding-top: 0;
    margin-top: 0;
  }
}
```

-----

**10. TYPOGRAPHY REFINEMENTS**

Current state: Good, but needs minor adjustments
Required state: Perfect hierarchy

```css
/* Hero headline */
.hero-headline {
  font-size: 72px; /* Desktop */
  font-weight: 700;
  color: #FFFFFF;
  line-height: 1.1; /* Tight */
  letter-spacing: -0.02em; /* Slightly tight */
  max-width: 900px;
  margin: 0 auto;
}

/* Hero paragraphs */
.hero-paragraph {
  font-size: 20px; /* Desktop */
  font-weight: 400;
  color: #A0A0A0;
  line-height: 1.6;
  max-width: 800px;
  margin: 0 auto;
}

/* Trust line */
.hero-trust-line {
  font-size: 14px;
  font-weight: 400;
  color: #71717A;
  line-height: 1.5;
  text-align: center;
  max-width: 700px;
  margin: 0 auto;
}

/* Mobile adjustments */
@media (max-width: 768px) {
  .hero-headline {
    font-size: 48px;
  }
  
  .hero-paragraph {
    font-size: 18px;
  }
}
```

-----

=== IMPLEMENTATION CHECKLIST ===

Before you finish, verify EVERY item:

**Colors:**

- [ ] Button gradient: #2563EB → #7C3AED
- [ ] Logo: Pure white #FFFFFF
- [ ] Hero background: #0F0F0F
- [ ] Final CTA background: #0F0F0F
- [ ] All other sections: #000000
- [ ] With/Without left column: #71717A
- [ ] With/Without right column: #FFFFFF
- [ ] With/Without divider: #1A1A1A

**Spacing (all multiples of 8):**

- [ ] Hero padding: 120px top/bottom
- [ ] Section gaps: 80px minimum
- [ ] Hero headline to P1: 32px
- [ ] Hero P1 to P2: 24px
- [ ] Hero P2 to button: 64px
- [ ] Button to trust line: 16px

**Structure:**

- [ ] With/Without has vertical divider (desktop)
- [ ] With/Without contrast is OBVIOUS
- [ ] How It Works cards have no visible borders
- [ ] Video Library is narrower (700px vs 1100px)
- [ ] Final CTA is elevated (#0F0F0F)

**Typography:**

- [ ] Hero headline: 72px desktop, 48px mobile
- [ ] Hero body: 20px desktop, 18px mobile
- [ ] Step titles: 20px, not 16px
- [ ] Footer links: #A0A0A0, not white

**No Exceptions. No Approximations. Pixel Perfect.**

Build this now.

```
---

**COPY THIS ENTIRE BLOCK INTO REPLIT AGENT**

This will transform the current 70% correct page into an award-winning 100% correct page.

The key changes:
1. ✅ Blue-to-purple gradient buttons (sophisticated)
2. ✅ Logo pure white (correct)
3. ✅ Elevated backgrounds on hero + final CTA (depth)
4. ✅ Perfect spacing (breathing room)
5. ✅ With/Without strong contrast (hierarchy)
6. ✅ All details pixel-perfect (award-winning precision)

**After they implement this, show me the result and we'll do final polish if needed.**
```