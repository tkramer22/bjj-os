Perfect. Let me create the award-winning correction prompt for the pricing page.

-----

# BJJ OS PRICING PAGE - AWARD-WINNING CORRECTION PROMPT

Copy this entire prompt into Replit Agent to fix the pricing page.

-----

```
CRITICAL DESIGN CORRECTIONS FOR PRICING PAGE - Read this entirely before making ANY changes.

This pricing page needs the same award-winning treatment as the landing page. Every change listed here is MANDATORY and PRECISE. No approximations.

=== GLOBAL FIXES ===

**1. BUTTON GRADIENT (CRITICAL - MUST MATCH LANDING PAGE)**

ALL buttons on this page must use blue-to-purple gradient:

```css
/* Primary CTA button (Continue to Onboarding) */
.continue-button {
  background: linear-gradient(135deg, #2563EB 0%, #7C3AED 100%);
  border: none;
  box-shadow: none;
  color: #FFFFFF;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: 16px;
  padding: 18px 48px;
  border-radius: 0;
  cursor: pointer;
  transition: 150ms ease;
  
  &:hover {
    background: linear-gradient(135deg, #1D4ED8 0%, #6D28D9 100%);
  }
  
  &:active {
    background: linear-gradient(135deg, #1E40AF 0%, #5B21B6 100%);
  }
}

/* Apply button (referral code) */
.apply-button {
  background: transparent;
  border: 1px solid #2563EB;
  color: #2563EB;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: 14px;
  padding: 12px 32px;
  border-radius: 0;
  cursor: pointer;
  transition: 150ms ease;
  
  &:hover {
    background: linear-gradient(135deg, #2563EB 0%, #7C3AED 100%);
    border: none;
    color: #FFFFFF;
  }
}

/* Selected card button */
.select-button.selected {
  background: linear-gradient(135deg, #2563EB 0%, #7C3AED 100%);
  border: none;
  color: #FFFFFF;
}

/* Unselected card button */
.select-button:not(.selected) {
  background: transparent;
  border: 1px solid #1A1A1A;
  color: #A0A0A0;
}

.select-button:not(.selected):hover {
  border: 1px solid #2563EB;
  color: #2563EB;
}
```

-----

**2. SELECTED CARD BORDER (MUST USE GRADIENT)**

Current state: Solid blue border
Required state: Gradient border (matches button)

This is the sophisticated detail that makes it award-winning:

```css
.pricing-card.selected {
  background: #0F0F0F;
  padding: 40px;
  border-radius: 0;
  position: relative;
  
  /* Gradient border technique */
  background: 
    linear-gradient(#0F0F0F, #0F0F0F) padding-box,
    linear-gradient(135deg, #2563EB 0%, #7C3AED 100%) border-box;
  border: 2px solid transparent;
}

/* Unselected card */
.pricing-card:not(.selected) {
  background: #0F0F0F;
  padding: 40px;
  border: 2px solid transparent;
  border-radius: 0;
  transition: border 150ms ease;
}

.pricing-card:not(.selected):hover {
  border: 1px solid #1A1A1A;
}
```

This creates a gradient border that matches the button gradient. CRITICAL for cohesion.

-----

**3. PAGE BACKGROUND (MUST BE PURE BLACK)**

Current state: Might be elevated
Required state: Pure black #000000

```css
body,
.pricing-page,
.pricing-container {
  background: #000000; /* Pure black, NO elevation on this page */
}
```

Unlike the landing page, the pricing page stays in pure black void. No elevated sections. Maximum focus on the choice.

-----

**4. SPACING SYSTEM (PRECISION REQUIRED)**

Current state: Probably compressed
Required state: Award-winning breathing room

```css
.pricing-page {
  background: #000000;
  min-height: 100vh;
  padding: 80px 40px; /* NOT 60px, NOT 100px */
}

/* Page header */
.pricing-header {
  text-align: center;
  margin-bottom: 48px; /* Exact */
}

.pricing-headline {
  font-size: 48px; /* Desktop */
  font-weight: 700;
  color: #FFFFFF;
  line-height: 1.1;
  margin-bottom: 16px; /* Exact */
  letter-spacing: -0.01em;
}

.pricing-subheadline {
  font-size: 16px;
  font-weight: 400;
  color: #A0A0A0;
  line-height: 1.6;
  max-width: 600px;
  margin: 0 auto;
}

/* Pricing cards section */
.pricing-cards-container {
  margin: 48px 0; /* Top and bottom */
}

.pricing-cards-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 32px; /* Between cards */
  max-width: 800px; /* NOT 900px, more focused */
  margin: 0 auto;
}

/* Savings callout */
.savings-callout {
  margin-top: 16px; /* Below annual card */
  text-align: center;
}

/* Referral code section */
.referral-section {
  margin: 48px auto 0; /* Above is cards, below is CTA */
  max-width: 500px;
  text-align: center;
}

.referral-header {
  margin-bottom: 16px;
}

.referral-input-group {
  margin-bottom: 8px; /* Before success/error message */
}

/* Continue button section */
.continue-section {
  margin: 64px auto 0; /* Large gap from referral section */
  max-width: 400px;
  text-align: center;
}

.continue-button {
  margin-bottom: 24px;
}

.continue-footer-text {
  margin-bottom: 0;
}

/* Mobile adjustments */
@media (max-width: 768px) {
  .pricing-page {
    padding: 60px 24px;
  }
  
  .pricing-headline {
    font-size: 36px;
  }
  
  .pricing-cards-grid {
    grid-template-columns: 1fr;
    gap: 24px;
  }
}
```

ALL spacing must be multiples of 8px. No exceptions.

-----

**5. PRICING CARD INTERNAL STRUCTURE (REFINEMENT)**

Current state: Good structure but needs polish
Required state: Perfect hierarchy

```css
.pricing-card {
  background: #0F0F0F;
  padding: 40px;
  border: 2px solid transparent;
  border-radius: 0;
  position: relative;
  cursor: pointer;
  transition: 150ms ease;
  text-align: center;
}

/* Best Value badge (Annual only) */
.best-value-badge {
  position: absolute;
  top: 16px;
  right: 16px;
  background: rgba(37, 99, 235, 0.1);
  color: #2563EB;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: 12px;
  padding: 4px 12px;
  letter-spacing: 0.05em;
  border-radius: 2px;
  text-transform: uppercase;
}

/* Plan name */
.plan-name {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: 14px;
  color: #71717A;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  margin-bottom: 24px; /* Space before price */
}

/* Price */
.plan-price {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: 48px; /* Large, impactful */
  color: #FFFFFF;
  line-height: 1;
  margin-bottom: 8px;
}

/* Period */
.plan-period {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 400;
  font-size: 14px;
  color: #71717A;
  margin-bottom: 32px; /* Space before trial details */
}

/* Trial details */
.trial-details {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 400;
  font-size: 14px;
  color: #A0A0A0;
  line-height: 1.6;
  margin-bottom: 32px; /* Space before button */
  min-height: 48px; /* Keeps cards aligned even if text wraps */
}

/* Select button */
.select-button {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 700;
  font-size: 14px;
  padding: 12px 32px;
  border-radius: 0;
  width: 100%;
  cursor: pointer;
  transition: 150ms ease;
}

.select-button.selected::after {
  content: ' ✓';
  margin-left: 4px;
}
```

-----

**6. SAVINGS CALLOUT (MUST USE GRADIENT COLOR)**

Current state: Solid blue text
Required state: Gradient text (advanced CSS)

```css
.savings-callout {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 400;
  font-size: 14px;
  line-height: 1.5;
  margin-top: 16px;
  text-align: center;
  
  /* Gradient text (award-winning detail) */
  background: linear-gradient(135deg, #2563EB 0%, #7C3AED 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  
  /* Fallback for browsers that don't support gradient text */
  @supports not (background-clip: text) {
    color: #2563EB;
  }
}
```

This makes “That’s $12.49/month — save $30/year” have gradient text. Subtle sophistication.

-----

**7. REFERRAL CODE SECTION (REFINEMENT)**

Current state: Functional but not polished
Required state: Award-winning interaction states

```css
.referral-section {
  margin: 48px auto 0;
  max-width: 500px;
}

.referral-header {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 400;
  font-size: 16px;
  color: #FFFFFF;
  margin-bottom: 16px;
  text-align: center;
}

.referral-input-group {
  display: flex;
  gap: 16px;
  margin-bottom: 8px;
}

/* Input field states */
.referral-input {
  background: #0F0F0F;
  border: 1px solid #1A1A1A;
  color: #FFFFFF;
  font-family: 'JetBrains Mono', monospace;
  font-weight: 400;
  font-size: 16px;
  padding: 12px 16px;
  border-radius: 0;
  flex: 1;
  transition: 150ms ease;
}

.referral-input::placeholder {
  color: #71717A;
}

.referral-input:focus {
  outline: none;
  border: 1px solid #2563EB;
}

/* Valid state (after code applied) */
.referral-input.valid {
  border: 1px solid #22C55E; /* Green */
  background: #0F0F0F;
}

/* Invalid state */
.referral-input.invalid {
  border: 1px solid #EF4444; /* Red */
  background: #0F0F0F;
  animation: shake 0.3s ease;
}

/* Shake animation for invalid */
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-4px); }
  75% { transform: translateX(4px); }
}

/* Read-only state (after code applied) */
.referral-input:read-only {
  opacity: 0.7;
  cursor: not-allowed;
  border: 1px solid #22C55E;
}

/* Success message */
.referral-success {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 400;
  font-size: 14px;
  color: #22C55E;
  margin-top: 8px;
  text-align: center;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
}

/* Error message */
.referral-error {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 400;
  font-size: 14px;
  color: #EF4444;
  margin-top: 8px;
  text-align: center;
}

/* Mobile */
@media (max-width: 768px) {
  .referral-input-group {
    flex-direction: column;
    gap: 16px;
  }
  
  .referral-input,
  .apply-button {
    width: 100%;
  }
}
```

-----

**8. CONTINUE BUTTON SECTION (POLISH)**

Current state: Functional
Required state: Perfect

```css
.continue-section {
  margin: 64px auto 0;
  max-width: 400px;
  text-align: center;
}

.continue-button {
  /* Uses global button gradient from Fix #1 */
  width: 100%; /* Mobile */
  max-width: 400px; /* Desktop */
  display: block;
  margin: 0 auto 24px;
}

.continue-button:disabled {
  background: #1A1A1A;
  color: #71717A;
  cursor: not-allowed;
  opacity: 0.5;
}

.continue-footer-text {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 400;
  font-size: 14px;
  color: #71717A;
  line-height: 1.5;
}
```

-----

**9. MOBILE RESPONSIVENESS (CRITICAL)**

Current state: Might be broken
Required state: Perfect on all devices

```css
@media (max-width: 768px) {
  .pricing-page {
    padding: 60px 24px;
  }
  
  .pricing-headline {
    font-size: 36px; /* Smaller on mobile */
  }
  
  .pricing-cards-grid {
    grid-template-columns: 1fr; /* Stack cards */
    gap: 24px;
  }
  
  .pricing-card {
    padding: 32px; /* Slightly less padding */
  }
  
  .referral-input-group {
    flex-direction: column; /* Stack input and button */
  }
  
  .continue-button {
    width: 100%; /* Full width on mobile */
  }
}
```

-----

**10. INTERACTION REFINEMENTS (AWARD-WINNING DETAILS)**

These micro-interactions separate good from award-winning:

```css
/* Card hover states */
.pricing-card:not(.selected):hover {
  border: 1px solid #1A1A1A;
  transform: translateY(-2px); /* Subtle lift */
  transition: all 150ms ease;
}

/* Button press feedback */
.select-button:active,
.apply-button:active,
.continue-button:active {
  transform: translateY(1px); /* Subtle press */
}

/* Focus states for accessibility */
.select-button:focus,
.apply-button:focus,
.continue-button:focus {
  outline: 2px solid #2563EB;
  outline-offset: 2px;
}

.referral-input:focus {
  outline: none; /* Custom focus via border color */
}

/* Smooth transitions everywhere */
* {
  transition-timing-function: ease;
  transition-duration: 150ms;
}
```

-----

**11. TYPOGRAPHY POLISH (FINAL TOUCHES)**

Ensure perfect text rendering:

```css
body {
  font-family: 'JetBrains Mono', monospace;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
}

/* Prevent text selection on UI elements */
.pricing-card,
.select-button,
.apply-button,
.continue-button {
  user-select: none;
  -webkit-user-select: none;
}

/* Allow selection on actual content */
.pricing-headline,
.pricing-subheadline,
.plan-name,
.trial-details,
.continue-footer-text {
  user-select: text;
  -webkit-user-select: text;
}
```

-----

=== IMPLEMENTATION CHECKLIST ===

Before you finish, verify EVERY item:

**Buttons & Gradients:**

- [ ] Continue button: Gradient #2563EB → #7C3AED
- [ ] Apply button: Transparent with gradient on hover
- [ ] Selected card button: Gradient background
- [ ] Selected card border: Gradient border technique
- [ ] Savings callout: Gradient text

**Colors:**

- [ ] Page background: Pure black #000000
- [ ] Card background: #0F0F0F
- [ ] Text primary: #FFFFFF
- [ ] Text secondary: #A0A0A0
- [ ] Text tertiary: #71717A
- [ ] Success: #22C55E
- [ ] Error: #EF4444

**Spacing (multiples of 8):**

- [ ] Page padding: 80px vertical, 40px horizontal
- [ ] Header to cards: 48px
- [ ] Cards to referral: 48px
- [ ] Referral to CTA: 64px
- [ ] Inside cards: 24px, 32px, 40px (as spec’d)

**Typography:**

- [ ] Headline: 48px desktop, 36px mobile
- [ ] Card price: 48px
- [ ] All using JetBrains Mono
- [ ] Weights: 400 (regular) and 700 (bold) only

**Interactive Details:**

- [ ] Cards hover with subtle lift
- [ ] Buttons have press feedback
- [ ] Invalid code shakes input
- [ ] All transitions 150ms ease
- [ ] Focus states visible

**Mobile (<768px):**

- [ ] Cards stack vertically
- [ ] Referral input/button stack
- [ ] All spacing adjusted
- [ ] Full-width CTA button

**No Exceptions. No Approximations. Award-Winning Precision.**

Build this now.

```
---

**COPY THIS ENTIRE BLOCK INTO REPLIT AGENT**

Key award-winning details:
1. ✅ Gradient buttons (matches landing page)
2. ✅ Gradient border on selected card (sophisticated)
3. ✅ Gradient text on savings callout (advanced)
4. ✅ Shake animation on invalid code (delightful)
5. ✅ Subtle hover lift on cards (premium feel)
6. ✅ Perfect spacing system (breathing room)
7. ✅ All interactions polished (press feedback, focus states)

**After they implement, show me the result and we'll do final polish.**
```