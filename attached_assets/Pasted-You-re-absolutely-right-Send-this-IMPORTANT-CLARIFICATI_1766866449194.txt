You’re absolutely right. **Send this:**

```
IMPORTANT CLARIFICATION: WEB/MOBILE vs iOS TESTING

I see the test results but they only cover web/mobile. I need clarity on iOS specifically.

═══════════════════════════════════════════════════════════════════════════════
THE TWO PARTS OF THIS PROJECT
═══════════════════════════════════════════════════════════════════════════════

PART 1: WEB/MOBILE APPLICATION
- Runs in browser or Replit preview
- You CAN test this directly
- Changes deploy instantly via "Publish"

PART 2: iOS APPLICATION  
- Capacitor wrapper around the web app
- Requires rebuild on my Mac (Xcode + Simulator)
- Has had persistent issues with changes NOT appearing

═══════════════════════════════════════════════════════════════════════════════
iOS-SPECIFIC ISSUES THAT REMAIN UNVERIFIED
═══════════════════════════════════════════════════════════════════════════════

1. Settings toolbar icons - text only, no icons
2. Chat persistence - disappears on app restart
3. Server URL loading - app ignores capacitor.config.ts server URL

These issues ONLY affect iOS. Your tests don't verify these are fixed.

═══════════════════════════════════════════════════════════════════════════════
QUESTIONS
═══════════════════════════════════════════════════════════════════════════════

1. Are the iOS fixes in BUNDLED mode or REMOTE mode?
   - Bundled = fixes compiled into the app
   - Remote = app loads from bjjos.app (but this hasn't been working)

2. Did you verify the capacitor.config.ts server URL is actually being used?

3. For the Settings toolbar icons fix - is this in:
   - ios-profile.tsx (iOS specific)?
   - Or a shared component?

4. For chat persistence - does the fix work on APP RESTART, not just tab switching?

═══════════════════════════════════════════════════════════════════════════════
WHEN I'M BACK AT MY MAC
═══════════════════════════════════════════════════════════════════════════════

I'll do a full iOS rebuild and test:
1. Settings toolbar icons
2. Chat persistence on app restart
3. Duplicate messages
4. Splash screen logo size
5. App icon

But I need confidence the iOS-specific code is actually fixed before I rebuild again.
```